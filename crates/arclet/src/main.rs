//! stellarc arclet — node daemon (ADR 0002).
//! Owns: host effects, harness adapters, workspace mgmt, local claim journal,
//! iroh endpoint. Reports claims; the plane holds truth (D14/D15).

fn main() {
    println!("arclet — scaffold");
}
