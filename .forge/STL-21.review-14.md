# STL-21 review (cycle 14) — PR #57 (forge/stl-21 @ dca27869, +5835/−132, 72 files)

Reviewer basis: PR diff + isolated worktree at head dca27869 (0 commits behind dev — rebase fixed). Unit, full integration, typecheck, biome, and three negative-control replays executed by reviewer on that tree.

## VERDICT: REWORK

## Per-item audit

| # | Item | Verdict |
|---|---|---|
| 1 | Spec compliance | **DEVIATED — same structural carve-out as c4/c13.** Delivered: `project`/`project_slug_alias`/`project_milestone`/`project_update` tables with verbatim fork constraints (incl. `completed_by` FK `restrict` — fork 0074 verbatim, spec text wrong); CRUD/slug-alias/archive + milestones + updates service→HTTP→event→collections→frozen UI; one-row transactional importer with preflight/idempotence; 4 sync collections; e2e parity for overview/detail/create-modal/archive/updates ×4 viewports. Missing: 4 of 8 tables, resource-links (T08 "gate item"), project-tickets (T10), grants/chokepoint extension (T14), privilege predicates, T17 all-14 + three-clean-imports. All deferred "wave-2" by implementer self-grant (.forge-question.md Q1/Q2) — third review, still no orchestrator spec amendment. Scope creep: none (CI PGDG + build-flag + timeout bumps are legitimate infra repair). |
| 2 | Tests that cannot fail | **PASS for what exists; one landmine.** T02d sabotage (expected 1→999): RED `AssertionError: expected 1 to be 999` → restored GREEN (76/76). T15c sabotage (`Effect.fn(name)`→`"sabotaged"`): RED `expected 0 to be greater than 0` / `toContain("Projects.createProject")` → restored 3/3 GREEN. T01a sabotage (drop `project_status_check`): RED at `expect(byName.project_status_check).toContain("planned")`. But `T01b` (projects.test.ts:82) **asserts the missing tables** — locks the carve-out in as correctness and will break the day wave 2 lands. |
| 3 | Migrations | **PASS.** Only `0008_projects.sql` added; journal appended (2→3, foundation/identity tests updated correctly); no shipped migration modified (`git tag --contains` empty); numbering 0008 with gap reserved — acceptable only under the invalid self-gating premise. |
| 4 | Doctrine | **PASS.** Single shared `appendProjectEventEffect` with span + actor everywhere; events atomic in the write tx (create/rename/archive verified in code); import emits `project:import-seeded` once per org with actor `'import'`; no hardcoded hex; no D4 model calls; no `console.*` in new code. |
| 5 | Worker debris | **PASS.** Tracked `.env` whitelist removed (`.env.example` + CI build flag instead — c13 D9 fixed); `tagOf` gone (biome EXIT 0, 2 infos only); no debug logs/stray files; screenshot diffs are new baselines. |
| 6 | Screenshots | **REWORK (narrower than c13).** New since c13: create-modal, after-archive, include-archived (desktop/tablet), updates-tab, updates-published ×4 projects; tablet `hasTouch`, mobile/mobile-small `hasTouch+isMobile` genuinely exercised; live-API interception-proof counters (`projectRequests() > hitsBefore`) real. Still missing: **rename-slug dialog** (spec §6 names it; `grep rename e2e/projects.spec.ts` = 0) and tickets/resource-link surfaces (impossible — features absent; subsumed by defects 2–3). |
| 7 | Spans (ADR 0010) | **PASS (delivered paths).** All 13 services via `opService`→`Effect.fn("Projects.*"/"ProjectMilestones.*"/"ProjectUpdates.*")`; `http.route/method/status` + `stellarc.org` + `principal.kind` (fail-closed ≥400, principal only on success); `db.*` spans, no statement text; span assertions go red on instrumentation removal (sabotage-proven). |
| 8 | Gates re-run (reviewer) | **PASS.** `bun run test:unit` 23/23. `bun run test:integration` **76/76 (7 files)** clean (first run's 1 failure was my own mid-run sabotage — reproduced RED in isolation, then clean full-suite green). `tsc --noEmit` exit 0. `biome ci .` EXIT 0. UI tsc 429 == recorded budget (`ui-typecheck-budget` fail is expected-red by design). PR CI `foundation`: pass @ dca27869. |

## DEFECTS (blocking, ordered)

1. **4 of 8 spec tables missing** — `packages/db/migrations/0008_projects.sql:2-4` creates only `project`, `project_slug_alias`, `project_milestone`, `project_update`; `project_board`, `project_repo`, `project_table_link`, `project_ticket` absent (FK targets task/board/repo/data_table unmerged on dev). Spec §2 mandates all 8 with verbatim constraints. Fix: rebase onto merged wave-2 and land all 8, or orchestrator amends the spec/splits the ticket — not another silent self-carve-out.
2. **Resource-link surface absent** — no `GET/POST/PUT/DELETE /:id/resources*` routes, no `packages/domain/src/project-links.ts`, no safe-summary union, no 409 `resourceType='table'` (spec §3, §5; T08 is "the gate item"). Fix: implement post wave-2 merge or formal descope.
3. **Project-ticket surface absent** — no `/:id/tickets*` routes, no `project-tickets.ts`, no progress computation (§3, T10); only pre-existing frozen UI fetchers. Fix: as 2.
4. **Grants/chokepoint extension absent (T14)** — STL-20 grant + org-privilege endpoints do not accept `resourceType=project`; no resolution chain role → grants ∪ team → `project.org_privilege` → org default. Fix: extend the seam when STL-20 lands, or descope via spec.
5. **Sync collections 4 of 6 and privilege predicates unwired** — `packages/sync/src/projects-shapes.ts:13-14` registers `project`, `project_slug_alias`, `project_milestone`, `project_update` only; `project_resource_link`/`project_ticket` unregistered; snapshot+tail are org-scoped but carry NO caller-privilege predicate (spec §4 requires privilege-filtered snapshot AND tail, identical, revocation drops rows+handles). Fix: wire predicates + 2 collections post wave-2.
6. **T17 final gate not executed** — all-14 reconciliation + three consecutive clean full imports absent; only the supplementary projects fixture runs (T02d — now genuinely executed and sabotage-red-able, but it is 1/14 + 0/3). Fix: land the all-14 harness (canon from merged STL-27) or record orchestrator deferral in the spec.
7. **Rename-slug dialog has no e2e/screenshots** — spec §6 names "archive/unarchive and rename-slug dialogs"; only archive/unarchive covered (`apps/stellarc-ui/e2e/projects.spec.ts`, zero rename tests). Fix: add spec + 4-viewport baselines — deliverable NOW, no wave-2 dependency.
8. **T01b locks in the gap** — `tests/integration/projects.test.ts:82` asserts satellite tables are absent; green today only because the spec is unmet. Fix: delete when defect 1 lands; meanwhile mark `test.fixme` with the wave-2 ref so it cannot masquerade as spec coverage.
9. **4 of 14 event types can never fire** — `project:resource-link-upserted/-deleted`, `project:ticket-linked/-unlinked` exist only as contract schemas (unit P4) — subsumed by defects 2–3; fix with them.

## Not defects (checked, clean)

`completed_by` FK `restrict` (fork 0074 verbatim — spec prose wrong, fork wins per §2); no-leak 404 identical bodies; alias-namespace collision; edit-history append-only; import idempotence zero-events; CI PGDG/PG15 apt fix; `VITE_API_URL=/api` build flag + tracked `.env.example` (c13 D9 resolution); expected-red `ui-typecheck-budget`; i18n `navigation:sidebar.projects` + `projects:*` present in all 19 locales and schema.json.
