# STL-21 review (cycle 15) — PR #57 (forge/stl-21 @ b4c2feb1, 80 files, +5892/−133 vs merge-base ce36b27)

Reviewer basis: full diff from merge-base (two-dot vs dev includes 1 orchestrator-only commit, excluded), plus isolated worktree at head b4c2feb1 with unit, full integration, typecheck, biome, CI check-runs, and a fresh negative-control replay executed by the reviewer.

## VERDICT: REWORK

## Per-item audit

| # | Item | Verdict |
|---|---|---|
| 1 | Spec compliance | **DEVIATED — same wave-2 carve-out as c4/c13/c14; spec still unamended.** Delivered: 4/8 tables (`project`, `project_slug_alias`, `project_milestone`, `project_update`) with verbatim fork constraints; CRUD/slug-alias/archive + milestones + updates service→HTTP→event→collections; one-row transactional importer (preflight/ledger/idempotence); 4 sync collections; live-API e2e ×4 viewports incl. the new rename-slug loop. Missing (blocked, verified): resource-links T08 ("the gate item"), project-tickets T10, grants/chokepoint T14 (STL-20 has no PR at all), privilege predicates, T17 all-14 + three-clean-imports, 4/14 event types fireable. Wave-2 premise verified real: STL-16 PR 54 OPEN, STL-18 PR 56 OPEN, STL-27 canon unmerged. Scope creep: none (CI PGDG + `VITE_API_URL=/api` build flag + tracked `.env.example` + integration timeout 30s→120s are legitimate infra repair, c13 D9/c14 D10 resolutions). |
| 2 | Tests that cannot fail | **PASS for what exists.** Fresh replay this cycle (T17 sabotage, `projects_checked` 1→999): RED `1 failed (4)` in projects-import.test.ts → `git checkout` revert → GREEN `4 passed`. Every T-case names its red-maker (drop check-constraint, skip alias-namespace collision, unscoped collection key, removed `Effect.fn` — c14-replayed). T01b correctly `test.skip` with wave-2 ref (c14 D8 fixed — no longer asserts the gap as correctness). |
| 3 | Migrations | **PASS.** Only `0008_projects.sql` added; journal appended in `migrate.ts` (2→3; foundation T06 + identity-contracts updated in-step); `git tag --contains origin/dev` empty — no shipped migration touched; no wholesale journal reformats. 0003–0007 gap reserved for wave slices (ordered-list engine, not filename-driven) — acceptable. |
| 4 | Doctrine | **PASS.** Shared `appendProjectEventEffect` (span + actor, tx-bound) on every mutation; import seeds `project:import-seeded` once per org, actor `'import'`, inside the same tx; no direct-SQL control-plane writes bypassing services; no hardcoded hex; no model calls; no `console.*` (T15b static guard green). |
| 5 | Worker debris | **PASS.** No stray files/debug logs; screenshot diffs are new baselines; fetcher `Mutation<T>` unwraps documented at the adapter seam; comments cite review defects they resolve. |
| 6 | Screenshots | **REWORK — carried blockers only; all c15-achievable gaps closed.** New since c14: rename-slug test + `project-rename-slug.png` ×4 viewports (live PUT + alias re-resolution + redirect — c14 D7 fixed). Verified non-gaps: archive/include-archived absent on mobile is fork-faithful (`Table className="hidden md:table"` in project-list.tsx:48); tablet/mobile `project-detail.png` baselines unchanged despite the mounted properties form — CI `foundation` **success at b4c2feb1** proves ≤0.001 diff (form below fold on narrow viewports), and `project-detail-live.png` ×4 covers the live detail surface. Still missing (impossible pre-wave-2): tickets tab and resources-section surfaces. |
| 7 | Spans (ADR 0010) | **PASS (delivered paths).** All services via `opService`→`Effect.fn("Projects.*"/"ProjectMilestones.*"/"ProjectUpdates.*")`; `http.route/method/status` + `stellarc.org` + `principal.kind` fail-closed; `db.*` spans statement-text-free (T15a asserts); T15b static no-console; span assertions sabotage-proven (c14, suite green this cycle). |
| 8 | Gates re-run (reviewer, isolated worktree @ b4c2feb1) | **PASS.** Unit `vitest run` 23/23 (18.6s). Integration 7 files: **75 passed, 1 skipped** (923s — co-run with the stl-25-c6 lane on this host; per-file green). `tsc --noEmit` exit 0. `biome check .` EXIT 0 (2 infos). PR CI @ head: `foundation` success, `ui-typecheck-budget` fail = inherited dev ratchet noise (429==429, continue-on-error). Negative control RED/GREEN pasted in item 2. |

## DEFECTS (numbered, file:line, exact fix)

1. **4 of 8 spec tables missing** — `packages/db/migrations/0008_projects.sql:2-4` creates only the 4 dependency-free tables; `project_ticket`/`project_board`/`project_repo`/`project_table_link` absent (FK targets task/board/repo unmerged: STL-16 PR 54, STL-18 PR 56 OPEN). Spec §2 mandates all 8. Fix: orchestrator merges wave-2 first, then land the satellites + renumber — or amends the spec. Not implementer-fixable now.
2. **Resource-link surface absent (T08, "the gate item")** — no `GET/POST/PUT/DELETE /:id/resources*`, no `packages/domain/src/project-links.ts`, no safe-summary union, no `resourceType='table'` 409. Fix: implement post wave-2 merge (or formal descope).
3. **Project-ticket surface absent (T10)** — no `/:id/tickets*`, no `project-tickets.ts`, no progress computation. Fix: as 2.
4. **Grants/chokepoint extension absent (T14)** — STL-20 is not merged (no PR exists); `resourceType=project` acceptance + role→grants∪team→`org_privilege`→org-default chain unwired. Fix: extend when STL-20 lands, or descope via spec.
5. **Sync collections 4/6; privilege predicates unwired** — `packages/sync/src/projects-shapes.ts:7-11` registers 4 collections; `project_resource_link`/`project_ticket` unregistered; snapshot+tail org-scoped but carry no caller-privilege predicate and no revocation drop. Fix: with defects 2/4.
6. **T17 final gate not executed** — all-14 reconciliation (needs STL-27 canon: "obtain, never invent") + three consecutive clean full imports absent; only the supplementary 21-column fixture runs (1/14). Fix: execute after wave-2 + STL-27 merge.
7. **4 of 14 event types can never fire** — `project:resource-link-upserted/-deleted`, `project:ticket-linked/-unlinked` exist only as contract schemas. Subsumed by defects 2–3; fix with them.
8. **Importer preflight hard-fails on satellite-present destinations** — `tools/import-projects.ts:73-79` throws `Preflight failed: satellite tables present` — spec T02 says preflight checks "satellites **empty/absent**". Once wave-2 lands the satellites, this preflight makes every rerun (incl. T17's three-clean-imports, which runs all importers post-wave-2) fail. Fix NOW: accept satellites-present-but-empty (assert `count(*)=0` per satellite), reject only non-empty. **Only defect actionable this cycle.**

## Not defects (checked, clean)

`completed_by` FK `restrict` (fork 0074 verbatim); no-leak 404 identical bodies; alias-namespace collision; edit-history append-only; import zero-events rerun; frozen.spec detail assertion rescoping (scoped to `project-overview` testid — the properties form repeats successCriteria; form itself covered by the live rename test + `project-detail-live.png` ×4); `project-resolve` invalidation on rename; mobile list skip (fork `hidden md:table`); CI PGDG/PG15 fix; `VITE_API_URL=/api` + tracked `.env.example`; timeout bumps under co-run load; T01b skip annotation.

## Reviewer note for the orchestrator

Defects 1–7 are sequencing failures, not implementation failures: the spec's own §1 premise audit orders T7 after wave 2, yet cycles 1–15 were dispatched with STL-16/18/20/27 unmerged. Four consecutive reviews (c4, c13, c14, c15) have reached the same carve-out verdict. Grinding cycle 16 against this PR cannot clear defects 1–7. Either merge wave-2 first and re-dispatch, or amend the spec/split the ticket — otherwise this loop repeats forever. Defect 8 is the one commit the next implementer can land today.
