REWORK

PR 53 (forge/stl-15 @ eda1f82c) vs dev 493e74c (merge-base 5856c24). Diff audited directly; gates re-run in throwaway worktree /tmp/stl15-rv. Implementer's own .forge-notes-c4.md concedes spans/shapes/UI/screenshots/reconciliation are NOT done ("Remaining for c5") — the adversarial audit confirms those and finds defects the notes do not.

## Per-item verdicts

| # | Audit item | Verdict |
|---|---|---|
| 1 | SPEC COMPLIANCE | PARTIAL — §2 tables/events (landed via dev #37 + this PR), §3 routes present, importer present. §4 sync shapes: MISSING. §5 UI MODIFY list: UNTOUCHED (zero files under apps/stellarc-ui/src). §6 frozen UI/e2e/screenshots: MISSING. tools/import-identity.ts, identity-fixture.ts, identity-import/identity-telemetry integration tests: MISSING. Better Auth adapter does not power the identity services (raw SQL instead). Scope creep: none found. |
| 2 | TESTS THAT CANNOT FAIL | VIOLATED — replayed spec's T06 negative control (union instead of intersect in capabilities.ts): D2 tests stayed GREEN (3 passed). Root cause defect 4: auth path seeds structural grants from the key ceiling, making the intersection vacuous. Also createOrganization owner lockout (defect 7) is masked because tests hand-seed grants (identity-http.test.ts:59-80) instead of creating the org through the API. |
| 3 | MIGRATIONS | CLEAN — PR touches no migration files; 0002_identity.sql shipped to dev via #37; no journal reformat. `git tag --contains` n/a (no tags on it). |
| 4 | DOCTRINE | VIOLATIONS — (a) D12: authenticateApiKey performs domain writes (principal + identity_grant INSERTs) on the auth path in its own transaction with NO events (auth.ts:132-207); spec §2 requires grant lifecycle via events, only session/token/verification/rate-counter writes are event-exempt. (b) Revocation: removeMember leaves identity_grant rows for the removed user's human AND agent principals (mutations.ts:199-232) — the user's agent keys keep structural grants (T12 broken at domain layer). (c) Human capability is role ∩ structural (capabilities.ts:159-168) but spec says "membership role PLUS structural grant" — with createOrganization seeding only org:member (mutations.ts:107-110), a fresh org OWNER cannot update their own org (403 at identity-http.ts:1034). No hardcoded-hex, no SQL interpolation, actor=principal.id correct. |
| 5 | WORKER DEBRIS | MINOR — dead export AUTH_IMPL_VERSION (auth.ts:2); CJS require() in ESM shipped path (identity-http.ts:94); empty `advanced.database` config block (better-auth.ts:40-44); .forge-notes-c3.md/.forge-notes-c4.md/.forge-deps-added.md committed at repo root. |
| 6 | SCREENSHOTS | REWORK — no apps/stellarc-ui/e2e/identity.spec.ts, zero new screenshots for any surface or any viewport. Brief: desktop-only or absent = REWORK; here all are absent. |
| 7 | SPANS (ADR 0010) | VIOLATED — zero Effect.fn on any new service (identity/*, better-auth-adapter.ts, better-auth.ts, identity-http.ts, identity-context.ts, auth-http.ts). main.ts:53-59 dispatch routes /api/auth/* and /api/identity/* around the Effect telemetry middleware, so no http.route/method/status/stellarc.org/stellarc.principal.kind spans exist for any new endpoint. identity-telemetry.test.ts does not exist; no span assertions to go red. No console.* in new src (verified). |
| 8 | GATES RE-RUN | `bun install --frozen-lockfile` OK; `bun run lint` exit 0 (7 warnings); `bun run typecheck` exit 0; unit suites 44/44 GREEN (266.8s, real disposable PG); integration identity-http 14/14 GREEN, identity-auth + identity-migration 14/14 GREEN. Gates pass on what exists — the gap is what is tested, not whether it passes. |

Negative-control replay evidence (item 2): sabotaged capabilities.ts agent intersection (`if (structural.has(cap))` removed → union), ran `vitest run --config vitest.integration.config.ts tests/integration/identity-http.test.ts -t "D2"` → `Tests 3 passed | 11 skipped (14)` = control CANNOT go red. Restored via git checkout; re-ran full file → 14/14 GREEN.

## DEFECTS

1. **Sync shapes/collections missing (§4).** No packages/sync/src/identity-shapes.ts; packages/sync/src/index.ts:176 still serves only `sync_probe` — no identity table allowlist on /orgs/:org/v1/shape. Fix: register the nine public projections (organization, organization_member, organization_role, team, team_member, invitation, apikey, principal, user) as org-scoped, capability-filtered shapes; add apps/stellarc-ui/src/lib/identity-collections.ts + identity-client.ts wiring.
2. **Screenshots + e2e missing (§6/T32).** No apps/stellarc-ui/e2e/identity.spec.ts; no __screenshots__ additions at any viewport. Fix: add the spec and fork-baseline-reused PNGs for all four inherited viewports, sign-in/switcher/Members/Teams/Roles/Keys, Members live against built API + real PG.
3. **Spans absent (ADR 0010, T30/T31).** Fix: wrap every new/changed service function in Effect.fn("Identity.mutationName" etc.) incl. better-auth-adapter and importer; route /api/auth/* + /api/identity/* through the requestTelemetry middleware in apps/stellarc-api/src/main.ts:53-59 so http.route/method/status/stellarc.org/stellarc.principal.kind are recorded; create tests/integration/identity-telemetry.test.ts asserting a span per new path and proving redness when instrumentation is removed.
4. **Decorative D2/T06 control — vacuous intersection.** packages/domain/src/identity/auth.ts:132-207 mints principal + identity_grant rows FROM the key ceiling on every authentication, so structural ⊇ ceiling always; the intersection in capabilities.ts:150-163 can never remove anything. Fix: make authenticateApiKey read-only (see defect 5); agent structural grants exist only via createApiKey/importer mutations; then re-run the union sabotage and paste the RED.
5. **D12 mutation-without-event on the auth path.** auth.ts:132-207 writes principal and identity_grant rows (domain state) in its own transaction with no identity:principal-upserted/grant-upserted events. Rate-counter/session writes are exempt; grant/principal writes are not. Fix: remove all INSERTs from authenticateApiKey; read grants instead of minting them.
6. **Revocation hole — removed member's agent keys keep access (T12).** mutations.ts:199-232 removeMember deletes only organization_member; identity_grant rows for the user's human principal and every agent:* principal derived from their keys remain. Fix: in the same tx delete identity_grant for all principals of the removed user (human:userId and agent keys with reference_id=user) and emit matching identity:grant-deleted events.
7. **Spec deviation — human capability intersection + owner lockout.** capabilities.ts:159-168 intersects membership-role caps with structural grants; spec §2: "Humans use membership role plus structural grant" (plus, not ∩). Combined with mutations.ts:107-110 (createOrganization seeds only org:member), a newly created org's owner gets 403 on PATCH /api/identity/orgs/:org (identity-http.ts:1034 requires organization:update). Hidden only because tests hand-seed owner grants (identity-http.test.ts:59-80). Fix: humans = roleCapabilities ∪ structural (org:member gates visibility); or seed full owner grant set; and fix tests to create fixtures through createOrganization.
8. **No Effect Schema decode / no excess-write-key rejection (§3).** identity-http.ts uses ad-hoc validId/validName/validPermission; header comment (:29-31) claims excess-key rejection that no code performs; require() at :94 imports the permission vocabulary mid-handler. No validated email on invitation create. Fix: decode every write body through packages/contracts/src/identity schemas (excess keys rejected, email validated, bounded lengths), import statement vocabulary statically.
9. **Manifest files missing.** tools/import-identity.ts, tests/helpers/identity-fixture.ts, tests/integration/identity-import.test.ts, tests/integration/identity-telemetry.test.ts absent; importer verification exists only as unit-level preflight tests. tests/fixtures/identity-reconciliation.sql correctly NOT invented (canonical #1-#3 still orchestrator-blocked — keep as explicit blocker, do not silently drop T26-T28).
10. **UI MODIFY list untouched (§5).** Zero changes under apps/stellarc-ui/src (auth-client, organization hooks/fetchers, api-key mutations, team-hierarchy, invitation fetchers). Sign-in/org-switcher/Members are not wired to the new API at all.
11. **Better Auth adapter powers nothing (§3).** better-auth.ts:62 mounts only adminPlugin; fork uses organization/dynamic-role/team/API-key plugins; better-auth-adapter.ts (405 lines) is exercised only incidentally. Typed identity services bypass it with raw SQL. Fix: wire the organization/key adapter paths the spec names, or justify each omitted plugin route in the PR with spec text.
12. **Debris.** Dead export AUTH_IMPL_VERSION (auth.ts:2) — delete; empty advanced.database block (better-auth.ts:40-44) — delete; .forge-notes-*.md/.forge-deps-added.md at repo root — move under .forge/ or drop from the PR.
13. **Grant-state integrity across members/roles.** updateMemberRole changes role but neither revokes nor re-derives the user's grants (mutations.ts:259-307 region); deleteRole blocks in-use but role-permission changes don't propagate to effectiveCapabilities consumers (they re-read dynamic roles, OK) — the member-role→grant drift remains. Fix: recompute grants on role change in the same tx with grant-upserted/deleted events.

Gates re-run: lint 0 errors (7 warnings), typecheck clean, unit 44/44, integration identity-http 14/14, identity-auth + identity-migration 14/14 — all green on the shipped subset; the failures above are coverage/doctrine gaps the suites cannot see.
