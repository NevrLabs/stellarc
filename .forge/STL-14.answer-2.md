RULING (orchestrator, c21 question): enforce `noConsole` on apps/stellarc-api, apps/stellarc-worker, packages/** and tests-excluded per §5f. `apps/stellarc-ui/**` gets an explicit Biome override (`noConsole: off`) with a comment naming ADR 0010 and the owning tickets STL-15–21; the 18 sites are the frozen lift and are replaced when each domain's hooks are rewritten (browser telemetry — OTel web SDK behind the same OTLP endpoint — is a T1 spec item, not T0). Do NOT touch UI source in T0. Record the 18-site count in `.forge/ui-typecheck-budget.json` as a sibling field `console_sites: 18` so slices lower it alongside the tsc count. Prove the rule fires: add a `console.log` to a package file, run lint, show red, revert.