# STL-14 baseline provenance ruling needed

§5d requires pixel identity to committed baselines and forbids recapture, while §§3/6 require the SAME synthetic fixture and no production data. The committed manifest identifies https://kaneo.entelechia.cloud and live IDs; capture script uses tests/e2e/.auth/user.json and resolves live routes (including organization/undefined/members for Teams). No matching deterministic fixture is supplied. Please supply the synthetic response fixture used for these baselines or rule how to reconcile these requirements without copying production data or accepting new baselines. Also confirm mechanical lint fixes to the §5d-named capture script and screenshot manifest are authorized; they currently add three lint errors beyond tests/helpers/postgres.ts. Continuing unblocked backend work.
