# STL-18 review c25 — VERDICT: REWORK

PR #56 @ `aaadb93e` (forge/stl-18), base dev. Diff +5487/−46, 27 files, backend-only.
Gate: implement=pass @ 2026-09-17T12:19:06Z. Reviewer re-ran gates at PR head.

## Per-item audit

| # | Item | Verdict | Evidence |
|---|---|---|---|
| 1 | Spec compliance | **REWORK** | §2 tables/events ✓ (six tables exact catalog verified by T01; 12 event types, schema_version=1, payload shapes match). §3 routes implemented in `repository-http.ts` but **never mounted in production** (D1); A4 webhook receiver endpoint absent (D2). §4 shapes implemented but **not registered in prod** (D1). §5: 10/17 CREATE files present; 3 UI files absent (D3). §6 pixel-frozen surfaces: absent (D3). §7: T01–T11,T14 ✓/partial; T12 ✗, T13 ✗ (D4). A1 ✓ (0003 numbering), A2 ✓ (canonical #10 fixture, digest + is-null parity), A3 ✓ (plaintext parity, `-- SECRET` markers, exclusion tests), A4 partial (D2). Scope creep (benign): `packages/contracts/src/repository-http.ts` (HttpApi contract decomposition), `tests/helpers/repository-http-server.ts`, `tests/fixtures/kaneo-src-schema.sql` — legitimate test infra, not in manifest. |
| 2 | Tests that cannot fail | **PASS w/ note** | T01 catalog sabotage (removed `DEFAULT false` from `repo.is_private`): RED `× T01 0003_repository creates the exact six-table catalog`, revert GREEN (3/3). T11 span sabotage: removing the manual `"http.route"` annotation stays GREEN — `@effect/platform` `HttpLayerRouter.js:111` also annotates `http.route`, so the attribute is redundant with the platform; **removing the whole `repositoryTelemetry` middleware goes RED** (`AssertionError: expected [] to have a length of 1 but got +0`, then GREEN on revert) — the span assertion is genuine. Other suites: assertions are behavioral (row/event/txid counts, 401/404 paths), each names a concrete breaking change. |
| 3 | Migrations | **PASS** | `0003_repository.sql` is new, append-only (123 insertions, no deletions); `MIGRATIONS` list appended in order; no shipped migration touched (`git diff dev..HEAD -- packages/db/migrations/` = new file only). Existing-test updates in `foundation.test.ts` (2→3 versions) and `identity-contracts.test.ts` are mechanical consequences of a new migration, registry-order-derived. |
| 4 | Doctrine | **PASS** | All mutations transactional with same-tx event append; actor required (`Invalid principal` guard); secrets excluded from events (`assertNoSecrets`), DTOs (`GrantPublic` rejects token fields), shapes (no grant collection), spans (telemetry tests assert no `gho_` marker/PII/SQL text). A3 plaintext parity honored with markers. No model calls, no direct-SQL bypass beyond the T0-owned postgres pattern. |
| 5 | Worker debris | **PASS** | No console.* in domain/api/sync code (tools CLI matches `migrate.ts` convention); no stray/debug files; diff is dense implementation, no generated noise. `.forge-question.md` correctly filed (c25 brief referenced a DEFECTS list that never existed — orchestrator should confirm and attach findings next cycle). |
| 6 | Screenshots | **FAIL** (D3) | `git diff dev..HEAD -- apps/stellarc-ui/` is **empty**. Zero UI files across all 25 cycles; no `repository-collections.ts`, no `repository-client.ts`, no `e2e/repository.spec.ts`, no new `__screenshots__` for any viewport. PR body's "UI collections" claim is contradicted by the tree. Riding fork baselines ≠ T12 evidence: the acceptance path (built API → live shapes → sidebar/issues/PRs) was never exercised. |
| 7 | Spans (ADR 0010) | **PASS w/ note** | 19 new `Effect.fn` sites (Domain.*, stellarc.event.append, stellarc.http.request, stellarc.github.*); http.route/method/response.status/stellarc.org/principal.kind present; telemetry test asserts trace-join (request↔service same traceId) and absence of SQL text/PII — verified RED on middleware removal. Note D5: platform emits `http.route` independently, so per-attribute sabotage can't redden; hardening suggestion only. |
| 8 | Gates re-run | **PASS** | `bun run lint` clean (974 files); `bun run typecheck` clean; unit `repository.test.ts`+`identity-contracts.test.ts` 15/15 (5.68s); integration per-file: repository 3/3, service+shape 12/12, http 9/9, import+webhook 11/11, telemetry 4/4 (all pristine tree). Full foundation/identity suites not re-run under host load; the shared `migrate()` path is covered by repository.test.ts. |

## DEFECTS

1. **[BLOCKING] Repository routes + shapes never mounted in production.** `apps/stellarc-api/src/main.ts:10` imports only `foundationHandler`; `grep -rn repository apps/stellarc-api/src/main.ts apps/stellarc-api/src/http.ts` → zero hits. `repositoryHandler` (repository-http.ts:303) and `registerRepositoryShapes` (repository-shapes.ts) are imported **only** by `tests/helpers/repository-http-server.ts:8-17`. A running API 404s every §3 route and serves no repository tables on `/v1/shape`. Spec §5 MODIFY list explicitly names `http.ts`/`main.ts`. All green HTTP tests prove the test harness, not the shipped artifact. **Fix:** compose `repositoryHandler` and `registerRepositoryShapes(engine)` into `main.ts` (mirroring the T0 foundation wiring), add one integration test that boots the production server composition (not the test helper) and hits `/api/identity/orgs/:org/repos`.
2. **[BLOCKING] A4 webhook receiver endpoint missing.** Amendment A4: "T4 owns the GitHub webhook *receiver* endpoint (signature verify → event append)". Only the domain service exists (`githubWebhookEffect`, repository-webhook.ts:47); `ROUTES` (repository-http.ts:35-57) has no webhook path; PR body concedes "no HTTP route was invented". The service-level suite is good, but the endpoint is spec-mandated. **Fix:** add `POST /api/identity/orgs/:org/github/webhook` (raw body + `x-hub-signature-256` + delivery id) mounting `githubWebhookEffect`, wired in prod per D1, with a test through the real server.
3. **[BLOCKING] Entire UI deliverable absent.** §5 CREATE `apps/stellarc-ui/src/lib/repository-collections.ts`, `repository-client.ts`, `e2e/repository.spec.ts`; §6 sidebar repository section/picker/issues/PRs/integration settings across 4 viewports; §7 T12. `git diff dev..HEAD -- apps/stellarc-ui/` → empty. The "thinnest live path" acceptance (§1: authorized discovery → sidebar list → live issues/PRs) is unmet end-to-end. **Fix:** implement the collections/client against the live shape API, lift the fork's repository surfaces per the manifest, and capture four-viewport Playwright baselines with the built API + imported rows (no request interception).
4. **[MAJOR] T13 org-switch/logout disposal untested.** §4 "org switching disposes old handles and caches… cannot reveal prior-org rows"; §7 T13 sabotage "reuses an unscoped collection key". No test in repository-shape/service/http suites references org switch, disposal, or prior-org leakage (grep: zero hits). **Fix:** add a shape-level test — snapshot for org A, switch to org B with the same engine/handle namespace, assert no A rows and that a reused unscoped key fails (sabotage: drop the org scoping from a collection key → RED).
5. **[NOTE, non-blocking] Span-attribute redundancy.** Manual `"http.route"` annotation in `repositoryTelemetry` duplicates `@effect/platform`'s own annotation (HttpLayerRouter.js:111) — a single-attribute sabotage cannot redden T11 (verified). Full middleware removal does (verified RED→GREEN). Hardening option: assert a repository-specific attribute (e.g. `stellarc.principal.id`) that the platform does not emit, so per-attribute sabotage is detectable.

## Test-run evidence (reviewer-executed, pristine tree @ aaadb93)

- `bun run lint` → `Checked 974 files in 16s. No fixes applied.`
- `bun run typecheck` → clean (tsc --noEmit, no output)
- unit: `Test Files 2 passed (2), Tests 15 passed (15)`
- integration repository.test.ts: `1 passed (1), 3 passed (3)`; service+shape: `2 passed (2), 12 passed (12)`; http: `1 passed (1), 9 passed (9)`; import+webhook: `2 passed (2), 11 passed (11)`; telemetry: `1 passed (1), 4 passed (4)`
- Negative control T01 (sabotage `is_private boolean NOT NULL DEFAULT false` → `NOT NULL`): `× T01 0003_repository creates the exact six-table catalog` → revert → `3 passed (3)`
- Negative control T11 (remove `middleware: repositoryTelemetry`): `× T11 … AssertionError: expected [] to have a length of 1 but got +0` → revert → `4 passed (4)`

**Verdict: REWORK — 4 defects (3 blocking, 1 major), 1 note.**
