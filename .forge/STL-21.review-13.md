# STL-21 review (cycle 13) — PR #57 (forge/stl-21, +5786/−131, 73 files)

Reviewer basis: PR diff rebased locally onto current origin/dev (8942e21c; clean, 25 commits) — the PR branch itself is 17 commits behind dev and NOT rebased/pushed. All test runs below executed on the rebased tree.

## VERDICT: REWORK

## Per-item audit

| # | Item | Verdict |
|---|---|---|
| 1 | Spec compliance | **DEVIATED — massive.** Delivered ~half the slice (see DEFECTS 1–7). Wave-2 dependency (STL-15/16/17/18 all unmerged: task/board/repo tables absent from base) blocks the rest; the spec's own premise audit predicted this ordering and §2/§5 mandate all 8 tables ("constraints included"), all ~30 endpoints, 6 collections. Implementer chose silent carve-out via migration comment (`.forge-question.md Q1` reference) instead of spec revision or STOP. |
| 2 | Tests that cannot fail | **PASS for what exists.** Sabotage replays (in /tmp clone, worktree untouched): T01a — commented out `project_status_check` → RED `expect(byName.project_status_check).toContain("planned")` `1 failed`. T15c — stripped `Effect.fn` from `opService` → RED at `expect(...).toContain("Projects.createProject")` `1 failed`. Restored → green (in full-suite run). No decorative tests found in shipped files; every named sabotage target for T01–T07/T09/T11/T13/T15 is genuinely lethal. T08/T10/T12/T14/T16-sabotage/T17 have NO tests (features absent). |
| 3 | Migrations | **PASS.** Only `0008_projects.sql` added; no shipped migration touched (`git tag --contains` basis: pre-existing files unmodified in diff); journal appended (`stellarc_migration` 2→3, foundation.test.ts updated accordingly — correct, not wholesale). |
| 4 | Doctrine | **PASS.** All writes via domain service + tx; every mutation emits its event in the same tx (`project:created/updated/slug-alias-created` asserted in order); import emits projection-seed only, actor `'import'`, once per org inside the tx; no direct SQL outside services/importer contract; no hardcoded ids/hex; no model calls. `console.error` in main.ts:64 is pre-existing (fatal startup handler — allowed). |
| 5 | Worker debris | **PASS.** No stray files (`.forge-question.md` gitignored), no debug logs, no commented-out code; screenshot diffs are new baselines, not regenerations of unrelated screens. `.gitignore` + tracked `apps/stellarc-ui/.env` (`VITE_API_URL=/api`) is deliberate (e2e parity), documented in-file — acceptable but borderline (see 9). |
| 6 | Screenshots | **REWORK.** New baselines present for the §6 surfaces this slice exercises × 4 projects (desktop/tablet 9 each, mobile/mobile-small 7 each; mobile archive/after-archive correctly skipped — frozen list renders no sub-md archive affordance). Live-API parity genuinely proven (request counter asserts `/api/project` hits, `route.fallback()` only for identity shell). BUT: rename-slug dialog has NO e2e/screenshot (spec §6 names it); resource-link dialogs/tickets tab have none — defensible only under the wave-2 carve-out that itself is defect 1. Touch IS enabled on tablet/mobile projects; mobile-small is `isMobile`+`hasTouch`. |
| 7 | Spans (ADR 0010) | **PASS (delivered paths).** All 13 mutation services via `opService` → `Effect.fn("Projects.*"/"ProjectMilestones.*"/"ProjectUpdates.*")`; http.route/method/status + `stellarc.org` + `stellarc.principal.kind` on every projects route (telemetry middleware maps sub-paths); `db.*` spans via `timed()` wrapper — no statement text; T15a/T15c assert spans and go red on instrumentation removal (proved by sabotage). No `console.*`, no PII (only `stellarc.principal.id` header value, fail-closed on ≥400). |
| 8 | Gates re-run | **PASS (on rebased tree).** `bun run test:unit` → 23/23. `bun run test:integration` → **75/75 (7 files)** including pre-existing foundation/identity suites. `typecheck:ui` 429 diagnostics == recorded budget 429 (`ui-typecheck-budget` job is `continue-on-error` by design — the red ✗ on the PR checks list is expected-red, not a regression). PR-run CI `foundation` job: pass. |

## DEFECTS (blocking, ordered)

1. **4 of 8 spec tables missing** — `packages/db/migrations/0008_projects.sql:2-4` creates only `project`, `project_slug_alias`, `project_milestone`, `project_update`; `project_board`, `project_repo`, `project_table_link`, `project_ticket` absent (deferred "to wave 2" by self-granted carve-out). Spec §2 mandates all 8 with verbatim constraints. Fix: either land all 8 (FK targets exist after STL-16/18 merge — rebase onto merged wave 2 first) or get the orchestrator to amend the spec/split the ticket; do not ship a silent half.
2. **Resource-link surface entirely absent** (spec §3 `GET/POST/PUT/DELETE /:id/resources…`, §5 `project-links.ts`, T08 "the gate item") — no `packages/domain/src/project-links.ts`, no safe-summary union, no 409 `resourceType='table'`. Fix: implement after STL-16/18 merge or formally descope via spec amendment.
3. **Project-ticket surface absent** (§3 tickets endpoints, §5 `project-tickets.ts`, T10, progress computation) — only the UI fetchers (pre-existing in dev) exist. Same fix path as 2.
4. **Sync collections 4 of 6** — `packages/sync/src/projects-shapes.ts:203-206` registers `project`, `project_slug_alias`, `project_milestone`, `project_update`; `project_resource_link` and `project_ticket` missing (§4). Also **privilege predicates stubbed**: file comment states caller-privilege predicates "land with the STL-20 chokepoint this slice extends" — but §4 requires privilege-filtered snapshot+tail NOW (chokepoint extension is in-scope: STL-20 endpoints' `resourceType=project` un-blocking, T14 — all absent). Fix: extend the STL-20 seam (grants/org-privilege accept `resourceType=project`, resolution order role → grants ∪ team → `project.org_privilege` → org default) and wire predicates into snapshot AND tail (identical), or descope via spec.
5. **T17 final gate not executed** — spec §7 T17: all-14 reconciliation on production-snapshot fixture + three consecutive clean full imports. Only the supplementary projects round-trip fixture exists (`tests/fixtures/projects-reconciliation.sql`), and even that is not executed anywhere (grep: referenced only by itself + import test comments). #13/#14 canon (STL-27) is unmerged — but the obligation is to obtain it, not skip it. Fix: land the all-14 harness invocation (canon from merged STL-27) or get explicit orchestrator deferral recorded in the spec.
6. **PR not rebased onto dev** — head 445f354/1be659a7 sits 17 dev-commits back; my local rebase was clean (no conflicts), so this is pure push-neglect. Fix: `git rebase origin/dev && git push -f` (orchestrator or implementer) — every gate result above already proves the rebased tree is green.
7. **Events truncated to 10 live types** — spec §2 lists 14 (12 live + 2 import). Contracts define all 14 schemas (unit P4 passes), but resource-link/ticket events can never fire (surfaces absent). Subsumed by defects 2–4.
8. **rename-slug dialog unexercised** — §6 names "archive/unarchive and rename-slug dialogs"; e2e covers archive/unarchive only (`apps/stellarc-ui/e2e/projects.spec.ts` has no rename test). Fix: add spec + 4-viewport baselines.
9. **Tracked `.env` whitelisted in `.gitignore`** (`.gitignore:7`, `apps/stellarc-ui/.env`) — contains only `VITE_API_URL=/api` today, but whitelisting `.env` in a shared repo invites secret leakage. Fix: rename to a tracked `apps/stellarc-ui/.env.e2e` (or `.env.example`) and drop the ignore exception.

## Not defects (checked, clean)

No-leak 404 identical bodies (T03b asserts byte-equality of unknown vs cross-org); edit-history append-only; completion-pair check; alias-namespace collision (squat test T03/T04); import idempotence zero-events; upcaster fail-closed on unknown version; T13d unknown-collection 404; CI PG15 apt fix is legitimate infra repair.
