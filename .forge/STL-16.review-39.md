# STL-16 review — cycle 39 (PR #54, head dc135d0, base dev)

**VERDICT: REWORK**

Gate: latest implement (cycle 39, 2026-09-16T18:23:25Z) = pass. Reviewed the diff (25 commits, 26 files, +5.9k/-60) in the c39 worktree and an isolated sabotage worktree at `/tmp/stl16-r39`. This is the 4th review of this ticket line (reviews 4/6 adjudicated REWORK; D2/D4/D5/D7/D8/D1-backend/D6/D10 from review-6 are verified FIXED this cycle; D3 is not).

## Per-item audit

| # | Item | Verdict | Evidence |
|---|---|---|---|
| 1 | SPEC COMPLIANCE | **FAIL** | Backend: 8 tables/86 cols exact (counted per-table from 0003_work.sql: board 16, alias 5, column 10, task 21, label 8, template 6, flag_type 8, task_flag 12 = 86), all §3 endpoints present (WORK_ROUTES 41 templates), taxonomy/ticket-key/modules per manifest, importer with preflight+digest ledger+readonly source (`postgres(src,{readonly:true})`), sync engine serves all 8 org-scoped collections, public endpoint minimal+rate-limited. **Still missing: §4 "five frozen views read live collections" and §6 pixel-frozen surfaces on live data (T28–T34)** — `work-collections.ts` has ZERO consumers under `apps/stellarc-ui/src/`; `board.tsx` still imports `useGetTasks` (react-query T0 stub); `apps/stellarc-ui/e2e/work.spec.ts` absent. Implementer's own tail admits: "Remaining: D3 only … 3–5h, beyond remaining budget." No orchestrator deferral exists (PR #54: 0 comments; issue #16 last comment is the implementer tail; no answered .forge-question.md). Scope additions beyond §5 manifest, all benign: `tests/integration/work-http.test.ts`, `packages/sync/src/work-upcasters.ts` (split), vitest/gates timeout bumps, registry-driven rewrites of foundation/identity-contracts assertions, `work_import` ledger table (mandated by digest-ledger requirement). Migration numbered 0003 not 0002 — correct post-STL-15 rebase; not billed. `tests/fixtures/work-reconciliation.sql` absent — spec-compliant (canonical #4–#6 SQL still unsupplied; correctly not invented). |
| 2 | TESTS THAT CANNOT FAIL | **PASS (2 minor gaps)** | 57 work tests (24+8+7+5+13). Negative controls I replayed myself: (a) spec T10 — `if (!valid.includes(status))` → `if (false && …)` in `setTicketStatus` (work.ts:1025): full-file run `Tests 3 failed | 21 passed` RED; restore → `24 passed` GREEN. (b) T26 spans — rename `Effect.fn(\`Work.${name}\`)` → `X.${name}` (work-http.ts:144): `2 failed | 3 passed` RED; restore → `5 passed` GREEN. T27 delete-txid now asserts the event row carries the envelope txid (work-http.test.ts:349) — fixed from review-6. T22 exactly-once reconnect race test present and strengthened (implementer found+fixed a real tail-cursor bug, dc135d0). Gaps: T24's 429 path has no test; T37 supplementary invariants (below) partial. `work.test.ts` is order-dependent — `-t T10` filtering reds on the T09-seeded `t-heal` fixture (full-file green). |
| 3 | MIGRATIONS | **PASS** | Only `0003_work.sql` added; 0001/0002 byte-identical to dev; `migrate.ts` appends to the ordered registry; journal appended, never reformatted (checksum drift rejection intact). No shipped migration touched; `git log origin/dev` confirms 0002_identity landed via #37 before this branch. |
| 4 | DOCTRINE | **FAIL** | Good: every mutation = service write + projection + event + counter on ONE postgres.js transaction (`runTx`/`emitInTx`); no post-commit second connection; no direct-SQL write path outside services; no hardcoded hex (IDs are caller-supplied/UUID); no model calls. Review-6 D4/D5 holes fixed (org predicates on `assignLabelTask`/`deleteLabel`; `listLabels`/`listTemplates` reject foreign org — tested work.test.ts:516/547). **Failures:** (a) prod actor = "anonymous" — see Defect 3; (b) `work:board-key-deleted` declared in contracts (work-events.ts:45) and shape mapping (work-shapes.ts:86) but no emitter/path exists anywhere — half-dead event contract. |
| 5 | WORKER DEBRIS | **PASS** | Zero TODO/FIXME/debugger in new sources; no commented-out blocks; review-6 debris (`work-unit-postgres.ts`, dead `PROJECTIONS.where`, forge-question force-add) all removed; `.forge-blocker.md` pre-exists on dev (driver artifact, not this PR). Diffs sized sanely. |
| 6 | SCREENSHOTS | **FAIL — REWORK** | Zero Playwright runs, zero screenshot diffs, `work.spec.ts` missing. Fork-provenance baselines exist (21 PNGs × 4 viewports incl. kanban/list/backlog/ticket-detail/org-shell, manifest pins kaneo@2504e645) but T34 parity (built API + real isolated PG + live collections, maxDiffPixelRatio 0.001) was never executed because the screens still read T0 stub data. Not "desktop-only" — nothing at all. This is Defect 1's other half. |
| 7 | SPANS (ADR 0010) | **PASS w/ deviation** | Every work endpoint handler is wrapped in `Effect.fn(\`Work.<method>\`)` (work-http.ts:130–149) inside the `stellarc.http.request` trace; route/method/status/stellarc.org/principal.kind asserted (telemetry tests 1–2); ≥1 span assertion per new path incl. per-aggregate coverage test; my rename sabotage proves the assertions redden (above). Deviation: domain functions are plain async (0 `Effect.fn` in work.ts) — spans live at the HTTP boundary, not on each service method. Acceptable reading (postgres.js domain layer has no Effect runtime; test-path calls keep working), but it is a letter-vs-spirit deviation — noted, not billed. T25 allowlist test scans span attrs + logs for title/description/note markers and `db.query.text` — green. `console.*`: 7 sites in `tools/import-work.ts` only (Defect 7, minor); none in runtime code. |
| 8 | GATES RE-RUN (mine) | **PASS** | c39 worktree, `bun install --frozen-lockfile` clean; `bun run typecheck` exit 0; `bun run lint` exit 0 (1 pre-existing unused-import warning); unit `28 passed (3 files)`; integration `95 passed (6 files, 677s)`; `bun test tests/gates.test.ts`: both gates pass (unit 38s, integration 682s) — one invocation showed a single integration-gate timeout (239s) while I was running a second suite concurrently on the same host; two isolated reruns green. CI on the PR remains unexercised (pre-existing PGDG apt failure on dev, not billed). |

## DEFECTS

1. **[BLOCKER] T28–T34 absent: five frozen screens not wired to live collections; no e2e; no parity evidence.** `apps/stellarc-ui/src/lib/work-collections.ts` (zero importers under `apps/stellarc-ui/src/`); `apps/stellarc-ui/src/routes/.../board/$boardSlug/board.tsx` still uses `useGetTasks` react-query stub; `apps/stellarc-ui/e2e/work.spec.ts` missing; 0 Playwright runs. **Fix:** implement the live-query bridge (implementer's own plan: `useSyncExternalStore` hook over the collections), wire sidebar-boards/kanban/list/backlog/ticket-detail, add `work.spec.ts` with T28–T34 incl. per-row negative controls, run 5 frozen screens × 4 viewports against fork-provenance baselines on built API + isolated PG — or obtain an explicit orchestrator-approved deferral (none exists today).
2. **`work:board-key-deleted` is a dead contract.** Declared at `packages/domain/src/work-events.ts:45` and mapped at `packages/sync/src/work-shapes.ts:86`, but `grep` over the branch finds no emitter and no alias-deletion service/endpoint — alias deletes can never stream. **Fix:** add the alias-removal path (service + endpoint) emitting `{id}` on one tx, with a test whose negative control deletes the emit; or, if alias deletion is genuinely out of scope this slice, remove the event from contracts + shape mapping so the contract doesn't lie.
3. **Production actor is `"anonymous"` on every work event.** `apps/stellarc-api/src/main.ts:49` passes `principalFrom = undefined`; `work-http.ts:155-156` defaults it to `() => ""`; `session()` (work-http.ts:61) then yields `principal: "anonymous"`, which is what every `emitInTx` writes as `actor`. Spec §2: "Actor = principal.id". Tests are green only because the test harness injects a real extractor (work-http.test.ts:35-40). **Fix:** pass the bearer-principal extractor (same grammar `workPrincipalOf` already implements) as `principalFrom` in `main.ts`, and add one assertion that a prod-composed handler stamps a non-anonymous actor.
4. **T37 supplementary invariants incomplete.** `tests/integration/work.test.ts` covers `StatusInUse` delete-blocking (T37-supp at :617) but not the two other mandated checks: "every non-virtual task status has a same-board column row" and "every column has a board". **Fix:** add both queries against the imported fixture (they must return zero rows) with a sabotage that inserts an orphan status/task and reddens them.
5. **T24 rate-limit path untested.** `tests/integration/work-http.test.ts:200` covers is_public/minimal-fields/404 but never exercises the 120/min fixed-window 429 (`RateLimited` + `retryAfterSeconds`). **Fix:** exceed the window in-test, assert 429 body; negative control = remove the bucket check.
6. **`work.test.ts` order-dependent.** Filtering (`-t`) reds tests that rely on earlier tests' fixtures (e.g. T10 needs `t-heal` created by T09's block at :102-127). Gate-safe (full-file runs) but breaks per-test sabotage workflow. **Fix:** self-seed fixtures per test (pattern already used by T23).
7. **`console.*` outside tests/fatal handler: `tools/import-work.ts` (7 sites: 23, 53, 62, 70-71, 74, 80).** Biome's `noConsole: error` override covers only api/worker/packages — tools/ is silently exempt. **Fix:** either add an explicit `tools/**` override decision in biome.json (allow, since a CLI's report IS its interface) or route through process.stdout/stderr writes; keep the fatal handler.

**Fixed-since-review-6 (verified this cycle, no action):** D2 import `{id,row}` emitters (work-import.ts:342-378); D4/D5/D7 org-scoping + transition-only emits (94e99f8, tests at work.test.ts:516/547/567); D8 board-ref HTTP resolution (a421e9e, work-http.test.ts:279); D1/D11/D12 ShapeEngine registration + real electric metadata + rate-bucket pruning (bbbb545); D6 spans (faae5ba, reddenable — I proved it); D10 T22/T27 strengthened + real tail-cursor bug found and fixed (dc135d0).

**Minor, not defects:** span placement at HTTP boundary vs per-function `Effect.fn` (deviation recorded above); gate/vitest timeout bumps to 900s/180s (justified by 11-min integration suite, but they mask slowness — watch it); T35/T36 correctly absent pending orchestrator-supplied canonical SQL (do not invent); `.forge-blocker.md` and `.forge/*.json` churn are driver artifacts.
