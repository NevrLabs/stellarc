VERDICT: REWORK

Review method: PR 60 head is **`a9a0c876` — byte-identical to review-8's audit**. Cycle-9 implement recorded "pass" (2026-09-18T06:18:59Z) but pushed **zero commits** (`origin/forge/stl-25` still a9a0c876; PR commit list unchanged, last commit a9a0c87 from cycle 8). This is the second consecutive phantom pass (cycle 7 did the same per review-7). The implementer's live worktree `/mnt/deepvault/forge-worktrees/stl-25-c9` sits at a9a0c87 with uncommitted changes to 5 files — `git diff -w` shows they fix only review-8 **D2 (lint)** and **D5 (principal.kind)**; `tools/measure-shape-concurrency.mts` and `tests/integration/shape-proxy.test.ts` (defect 1 files) are untouched there. The cycle-8 worktree that held the D1/D4 tool+test fixes was **deleted** — that fix is now nowhere. All replays below ran in my own clean worktree at exactly a9a0c876 (`/mnt/deepvault/forge-worktrees/stl-25-review-c9`, fresh `bun install --frozen-lockfile`, 747 pkgs).

## Per-item audit

| # | Item | Verdict |
|---|---|---|
| 1 | SPEC COMPLIANCE | **DEVIATED — no movement since review-8.** Carried unfixed at head: D1 (§5 tool + §7 S12): `tools/measure-shape-concurrency.mts:53` still probes `…&offset=-1&shape-id=N` — `shape-id` is not in the engine allowlist (every probe 400s) and `offset=-1` plain fetches complete immediately, holding no live HTTP/1.1 connection, so the harness measures nothing; the `--self-check` mode its header (line 13) and ADR 0012 §"Concurrent live-shape ceiling (S12)" (line 70) advertise does not exist; no S12 cap-2 integration test exists (`grep cap/limiting tests/integration/shape-proxy.test.ts` → 0). Everything else from review-8's item 1 stands: negotiation rule, allowlist + strict booleans, streaming branch (no `response.text()` on SSE path), per-page authorize + ka-tick re-authz, abort fan-in, byte-compatible long-poll, 20s cycle/15s ka, `X-Accel-Buffering: no`, ADR 0012, all 8 CREATE + 5 MODIFY files, T18 sibling edit. Scope creep carried: `.forge-blocker.md` deletion, root `playwright@1.62.0`, bun.lock churn. Zero UI files touched. |
| 2 | TESTS THAT CANNOT FAIL | **DEVIATED (carried).** S12 still exercised only via the pure `ceilingFromResults` unit; the real harness's blindness (defect 1) cannot redden any test. My replayed control (spec S01/S11): removed `"live_sse"` from the allowlist (`index.ts:350`) → `× S02 × S11 … Tests 2 failed | 7 passed (9)`; restored → `Tests 9 passed (9)`. Control is real; coverage of S12 is not. |
| 3 | MIGRATIONS | **PASS** — `git diff origin/dev...HEAD -- packages/db` empty; no migration commits; journal untouched. Spec §2 NONE honored. |
| 4 | DOCTRINE | **PASS (carried from review-8, unchanged files).** No SQL outside the engine; no new events/mutations; no model calls from control plane; no hardcoded tokens; span attrs table/org/principal.kind/offsets/close only — no statement text, no PII; no new `console.*`. |
| 5 | WORKER DEBRIS | **DEVIATED (process, worse than c8).** In-tree debris: none (review-7 leftovers stay cleaned). New: the cycle-9 pass attests an uncommitted tree — `stl-25-c9` holds D2+D5 fixes (+12/−10 semantically) that exist nowhere in the PR, and the c8 worktree containing the D1 tool rework (+122 lines) and S12 integration test (+55 lines) was deleted before anything was pushed. Fix work is being lost cycle over cycle. |
| 6 | SCREENSHOTS | **N/A-PASS (carried)** — zero UI source changes (nothing under `apps/stellarc-ui` in the diff), so no PNG deltas expected. Carried caveat: no run evidence for the frozen Playwright S16 leg; coverage rests on the no-UI-diff argument. |
| 7 | SPANS (ADR 0010) | **PASS with carried minor (D5).** `Effect.fn("Sync.sseEffect")`; per-connection `stellarc.shape.sse` span with table/offset_from/org/events_sent/close; page spans parented via `Tracer.ParentSpan` (S15 asserts `parentSpanContext.spanId`; review-8 proved it red under sabotage). Carried: `stellarc.principal.kind` hardcoded `"actor"` (index.ts:211 at head) — fix exists only uncommitted in stl-25-c9. |
| 8 | RE-RUN GATES | **FAIL at head.** Unit 27/27 ✓; `tsc --noEmit` exit 0 ✓; focused integration (shape-sse + shape-proxy) 12/12 ✓; negative control red/green ✓ (item 2). **`bun run lint` exit 1 — same 4 errors as review-8**: `tests/unit/shape-sse.test.ts:49:9 noUnusedVariables` + format errors in `packages/sync/src/index.ts`, `packages/sync/src/sse.ts`, `tests/integration/shape-sse.test.ts`, `tests/unit/shape-sse.test.ts`. The fix for this is sitting uncommitted in the implementer's worktree. Gates-wrapper integration timeout and the two inherited-red CI checks: unchanged from review-8 (inherited, not PR-caused). |

## DEFECTS (numbered, file:line, exact fix expected)

1. **[BLOCKER — carried D1, unchanged]** `tools/measure-shape-concurrency.mts:53` — probes with `shape-id=` (not in allowlist → 400) + `offset=-1` (no held connection); advertised `--self-check` mode (header line 13) not implemented; ADR 0012 §S12 (line 70+) claims a unit-tested cap-2 self-check that doesn't exist; no S12 integration test. The c8 rework (+122 tool lines, +55 test lines) was in a worktree that has been deleted. **Fix:** reimplement (snapshot-minted handle per shape, held qualifying SSE fetch, implemented `--self-check` cap-2 proxy mode, S12 integration self-check in `tests/integration/shape-proxy.test.ts`), **commit and push to `forge/stl-25`**, make ADR 0012's claim true or strike it.
2. **[BLOCKER — carried D2, unchanged at head]** lint red: `tests/unit/shape-sse.test.ts:49:9` unused `before` binding + 4 format errors (`packages/sync/src/index.ts`, `packages/sync/src/sse.ts:55-57`, both shape-sse test files). Fix exists uncommitted in stl-25-c9. **Fix:** apply those changes, `bun run lint` green, **push**.
3. **[BLOCKER — stage integrity, repeat offense]** Cycle-9 implement "pass" pushed zero commits; PR head unchanged from the REWORK it claims to address; cycle-8's fix work was destroyed with its worktree. **Fix:** implementer works in the worktree, commits AND pushes before claiming pass; orchestrator should re-gate implement against the pushed head (not worktree state) before re-review.
4. **[MINOR — carried]** `.forge-blocker.md` deleted by this PR (55 lines, not in spec manifest). **Fix:** orchestrator confirms deletion intended or restores.
5. **[MINOR — carried]** `packages/sync/src/index.ts:211` — `stellarc.principal.kind` hardcoded `"actor"`; handler-derived fix (`principalKindFrom` in http.ts + threading) exists uncommitted in stl-25-c9. **Fix:** push it.
6. **[MINOR — inherited, carried]** gates-wrapper integration leg dies at 300 s. **Fix:** raise timeout or split config — orchestrator action, not this PR.

## Appendix — replayed evidence (clean worktree at a9a0c876)

- `bun install --frozen-lockfile`: 747 packages.
- Unit: `Test Files 5 passed (5), Tests 27 passed (27)`.
- `bunx tsc --noEmit`: exit 0.
- Focused integration: `Test Files 2 passed (2), Tests 12 passed (12)` (88.8 s).
- Negative control (S01/S11): allowlist sabotage → `Tests 2 failed | 7 passed (9)` (S02, S11 RED); restored → `Tests 9 passed (9)`.
- `bun run lint`: `Found 4 errors. Found 1 warning.` exit 1 (defect 2).
- PR state: head `a9a0c876`, OPEN, 18 commits, last a9a0c87 (cycle 8).
