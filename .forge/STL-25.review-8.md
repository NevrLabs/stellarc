VERDICT: REWORK

Review method: full `gh pr diff 60` read (2,528 lines, 18 files) at PR head `a9a0c876` — the first review to actually audit this head (review-7 read the stale `5b5ce8c`; cycle-8 commits `178922b`/`085575f`/`47d3256`/`499e7a9`/`a9a0c87` landed minutes after it finished). All replays in a clean detached worktree at exactly `a9a0c876` (`/mnt/deepvault/forge-worktrees/stl-25-review-c8`, fresh `bun install --frozen-lockfile`, 747 pkgs) — NOT the implementer's tree, which holds unpushed changes (see defect 3). Review-7's D1/D2/D3/D5/D6 are verified fixed at head; D4 is NOT (fix exists only uncommitted). One NEW blocker: the lint gate is red at head.

## Per-item audit

| # | Item | Verdict |
|---|---|---|
| 1 | SPEC COMPLIANCE | **DEVIATED** — Fixed at head vs review-7: D1 (409 must-refetch passthrough, `178922b`), D2 (truthful fallback semantics + ka-gated first boundary, `085575f`; S10 green in isolation), D3 (frames counter split by `kind` attribute, `47d3256`). Still deviated: D4 — `tools/measure-shape-concurrency.mts` at head still probes with `shape-id=` (not in the engine allowlist → every request 400s) + `offset=-1` plain fetches that complete immediately and hold no connection; the `--self-check` mode the tool header and ADR 0012 §"Concurrent live-shape ceiling (S12)" advertise does not exist at head (`grep selfCheck` → 0). S12's RED condition ("point the harness at a conn-cap-2 proxy and assert it reports 2") is unmeetable as shipped. Present and correct: negotiation rule, allowlist + strict-boolean gate, streaming branch (no `response.text()` on the SSE path), per-page authorize + ka-tick re-authz, abort fan-in, byte-compatible long-poll, 20s cycle/15s ka, `X-Accel-Buffering: no`, ADR 0012, all 8 CREATE + 5 MODIFY files, T18 sibling edit. Scope creep (carried): `.forge-blocker.md` deleted (orchestrator confirm), `playwright@1.62.0` root dep (declared in `.forge-deps-added.md`, tolerable), bun.lock transitive churn. Zero UI files touched. |
| 2 | TESTS THAT CANNOT FAIL | **DEVIATED** — S12's unit test (`tests/unit/shape-concurrency.test.ts`) exercises only the pure `ceilingFromResults`; nothing at head ever points the real harness at the cap-2 proxy, so the harness's own blindness (defect 1) cannot redden any test. The fix + real S12 integration self-check exist unpushed (defect 3). Controls proven failable by my replays: allowlist sabotage → S02+S11 RED; `Tracer.ParentSpan` sabotage → S15 RED (`parentSpanContext.spanId` assertion, `499e7a9`); S10 passes in isolation (no order dependence); S01-conflict covers the 409 path. |
| 3 | MIGRATIONS | **PASS** — `git diff --stat origin/dev...HEAD -- packages/db` empty; zero migration commits; no journal touch. Spec §2 NONE honored. |
| 4 | DOCTRINE | **PASS** — no SQL outside the engine (sse.ts contains none; pages go via `pageEffect`); no new events/mutations; no model calls from control plane; no hardcoded tokens (`Bearer` literals are test grammar; the `***` I saw in one diff render was a display redaction — `od` shows `` `Bearer ${org}` ``). Span attrs: table/org/principal.kind/close/offsets only — no statement text, no PII. `console.*` in shipped code: only pre-existing `main.ts:64` startup error. |
| 5 | WORKER DEBRIS | **DEVIATED (process)** — in-tree debris from review-7 D5 is gone (`ConflictError`, `_untilRow`, `caughtUp` stubs all removed). New: the implementer's worktree `/mnt/deepvault/forge-worktrees/stl-25-c8` carries **uncommitted** D4 fixes (+122 lines tool, +55 lines test) and an untracked `docs/evidence/shape-concurrency.json` whose own content (ceiling 0, stalled 1, target `127.0.0.1:1`) proves the tool still measures garbage — none of it is in the PR. |
| 6 | SCREENSHOTS | **N/A-PASS** — zero UI source changes (verified across the 18-file diff; nothing under `apps/stellarc-ui`), so no PNG deltas are expected. Carried caveat: the frozen Playwright regression umbrella (S16 e2e leg) has no run evidence in the PR and I did not run it; coverage rests on the no-UI-diff argument. |
| 7 | SPANS (ADR 0010) | **PASS (minor carried)** — `sseEffect = Effect.fn("Sync.sseEffect")` (index.ts:162); per-connection `stellarc.shape.sse` span with table/offset_from/org/events_sent/close-kind; page spans parented via `Effect.provideService(Tracer.ParentSpan, span)` and S15 asserts `parentSpanContext.spanId` — I removed the pipe and S15 went RED (real control). Minor: `stellarc.principal.kind` hardcoded `"actor"` (index.ts:211) instead of derived from the handler principal (spec §2 "from the handler") — defect 5. No statement text/PII/console on new paths. |
| 8 | RE-RUN GATES | **PASS with two caveats** — unit 27/27; `tsc --noEmit` exit 0; focused integration 12/12 (incl. S01-conflict, S10); S10 isolated 1 pass; foundation 43/43; identity-migration 8/8; gates wrapper `1 pass / 1 fail` (integration leg killed at 300,056 ms — inherited, reproduces on dev). **`bun run lint` exit 1 at head** (defect 2). GitHub PR checks (`foundation`, `ui-typecheck-budget`) both fail — but both fail identically on dev HEAD `7170d3b` (foundation dies at "Install disposable PostgreSQL binaries"; ui-typecheck-budget: diagnostics 429 = budget 429 but compiler exit 2) → inherited, not PR-caused. |

## DEFECTS (numbered, file:line, exact fix expected)

1. **[BLOCKER — spec §5 tool + §7 S12; D4 carried, NOT fixed at head]** `tools/measure-shape-concurrency.mts:40-56` (at head: 133 lines) — `pageSource` probes `…/v1/shape?table=sync_probe&offset=-1&shape-id=N`: `shape-id` is not in the engine allowlist (every probe 400s) and `offset=-1` fetches complete immediately, holding no live HTTP/1.1 connection — the harness cannot measure any ceiling. Header lines 10-13 advertise a `--self-check` mode that does not exist (`grep selfCheck` → 0 at head), and `docs/adrs/0012-sync-transport-hardening.md` §"Concurrent live-shape ceiling (S12)" asserts it is "unit-tested" against a cap-2 proxy. The implementer's own local artifact (`docs/evidence/shape-concurrency.json`, untracked, 04:59:48Z) reports `ceiling: 0, stalledShapeId: 1`. **Fix:** push the reworked tool from the implementer worktree (snapshot-minted handle per shape, held qualifying SSE fetch, implemented `--self-check` cap-2 proxy mode; +122 lines) AND the S12 integration self-check test (+55 lines, `tests/integration/shape-proxy.test.ts`); rerun; make ADR 0012's claim true or strike it.
2. **[BLOCKER — repo lint gate red at head; NEW this cycle]** `bun run lint` (`biome check .`) exits 1 with 4 errors, all in PR files: `tests/unit/shape-sse.test.ts:49:9` `noUnusedVariables` (`before`), plus format errors in `packages/sync/src/index.ts` (~181), `packages/sync/src/sse.ts:55-57`, `tests/integration/shape-sse.test.ts`, `tests/unit/shape-sse.test.ts`. CI never surfaced it (the `foundation` job dies at PG-binary install first, inherited), but `package.json:13` lint is a CI step and will fail the moment that install is fixed. Introduced by cycle-8 commits (review-7 had biome clean). **Fix:** delete or use the `before` binding, run `biome check --write` on the four files, commit with lint verified.
3. **[MAJOR — stage integrity]** Cycle-8 implement recorded **pass** (2026-09-18T05:01:31Z, head `a9a0c876`) while the worktree that produced the evidence holds uncommitted D4 fixes and an untracked evidence JSON — the PR does not contain the code the pass attests. **Fix:** implementer pushes the worktree; orchestrator re-runs the implement gate against the pushed head before re-review.
4. **[MINOR — carried]** `.forge-blocker.md` deleted by this PR (STL-18-era note, not in the spec manifest). **Fix:** orchestrator confirms deletion is intended or restores it.
5. **[MINOR — carried, spec §2]** `packages/sync/src/index.ts:211` — span attribute `stellarc.principal.kind` hardcoded `"actor"` rather than derived from the handler principal. **Fix:** thread the principal kind into `sseEffect` alongside the authorize closure (default `"actor"` only as fallback).
6. **[MINOR — inherited, orchestrator action]** Gates wrapper integration leg killed at 300 s (foundation alone 274 s; this PR adds 12 integration tests). **Fix:** raise `tests/gates.test.ts` timeout or split the integration config — carried D7; as-is, "gates green" is unverifiable in one run.

## Appendix — replayed evidence (clean worktree at a9a0c876)

- Unit: `Test Files 5 passed (5), Tests 27 passed (27)`. `tsc --noEmit` exit 0.
- Focused integration: `Test Files 2 passed (2), Tests 12 passed (12)` (shape-sse + shape-proxy).
- S10 isolated: `1 passed | 2 skipped` — review-7 D2 order-dependence is gone at head.
- Foundation: `43 passed (43)`. Identity-migration: `8 passed (8)`.
- Negative control (spec S01/S11): removed `"live_sse"` from the allowlist → `× S02 … × S11 … Tests 2 failed | 7 passed (9)`; restored → `Tests 9 passed (9)`.
- Span control (S15): removed `.pipe(Effect.provideService(Tracer.ParentSpan, span))` → `× S15 … 1 failed | 8 skipped`; restored.
- Lint at head: `Found 4 errors. Found 1 warning.` exit 1 (details in defect 2).
- Gates wrapper: `(pass) Vitest gate: vitest.config.ts [16579ms]` / `(fail) Vitest gate: vitest.integration.config.ts [300056ms]`.
- CI at head (gh pr checks 60): `foundation fail`, `ui-typecheck-budget fail` — identical failures on dev HEAD `7170d3b` → inherited.
- Commits under review: `178922b` D1 fix, `085575f` D2 fix, `47d3256` D3 fix, `499e7a9` D6 fix, `a9a0c87` D5 cleanup.
