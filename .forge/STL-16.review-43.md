# STL-16 review — cycle 43 (PR #54, head d885961b, base dev)

**VERDICT: REWORK**

Gate: latest implement (cycle 43, 2026-09-18T15:34:35Z) = pass. Reviewed the diff (33 commits, 31 files, +8005/−54) in the detached worktree `/mnt/deepvault/forge-merge/review-stl-16-c43` (clean, HEAD = PR head). 5th review of this ticket line. r39 defects D2–D7 are all verified FIXED this cycle; D1 (the blocker) is not, and cycle 43's new code broke both repo gates.

## Per-item audit

| # | Item | Verdict | Evidence |
|---|---|---|---|
| 1 | SPEC COMPLIANCE | **FAIL** | Backend complete and unchanged-strong: 8 tables/86 cols exact (recounted from 0003_work.sql: board 16, alias 5, column 10, task 21, label 8, template 6, flag_type 8, task_flag 12), `milestone_id` no FK, all §3 endpoints, importer (readonly source, preflight, digest ledger), 8 sync collections, public endpoint. **Still missing: §4 live-collection wiring + §6 frozen screens (T28–T34).** Cycle 43 added `apps/stellarc-ui/src/lib/work-live-bridge.ts` + `work-view-model.ts` — but grep shows ZERO consumers under `routes/` or `components/`: `board.tsx:23` still imports `useGetTasks`, `nav-boards.tsx:43` still `useGetBoards`, backlog/task-detail likewise; `e2e/work.spec.ts` absent. The new libs are dead code with decorative unit tests ("T28 unit"). No orchestrator deferral (PR #54's only comment is the r39 review; issue #16's last comment is the implementer tail; no answered .forge-question). Scope additions all benign: work_import ledger table, work-upcasters split, work-http tests, `DELETE /api/work/board-keys/:id` (D2 fix), biome tools/** override, gate-timeout bumps. Migration numbered 0003 (spec said 0002) — correct post-STL-15; not billed. `tests/fixtures/work-reconciliation.sql` correctly absent (canonical #4–#6 SQL still unsupplied). |
| 2 | TESTS THAT CANNOT FAIL | **PASS** | 69 work tests (20 unit + 49 integration). Negative controls replayed by me on real disposable PG: (a) T10 — `if (!valid.includes(status))` → `if (false && …)` (3 sites, work.ts:871/1044/1162): `T10: invalid status` **1 failed** RED; restore → **1 passed** GREEN. (b) T26 — `Effect.fn(\`Work.${name}\`)` → \`X.${name}\` (work-http.ts:145): **2 failed** (span-name tests) RED; restore → **5 passed** GREEN. T10 standalone `-t` filtering now works — r39's order-dependence (D6) confirmed fixed by self-seeding (30722fd). New D2/D3/D5 tests redden by construction (they pin the exact previously-missing wiring). Gap: bridge/view-model unit tests can never go red against a real screen because no screen consumes them — moot until D1 lands. |
| 3 | MIGRATIONS | **PASS** | Only `0003_work.sql` added; `git diff origin/dev...HEAD -- 0001/0002` empty; `migrate.ts` appends to ordered registry; journal appended. New table not on any tag — unshipped. |
| 4 | DOCTRINE | **PASS** (first time) | r39 D2 fixed: alias-removal service (work.ts:742) + `DELETE /api/work/board-keys/:id` emits `work:board-key-deleted` {id} on one tx, test at work-http.test.ts:426. r39 D3 fixed: `composeWorkHandler` (main.ts:33) passes `workPrincipalFrom`; pinned by a test composing the handler exactly as prod does and asserting `actor !== "anonymous"` (work-http.test.ts:397–417). All mutations still `runTx` (write+projection+event+counter, one connection); no post-commit hooks; no direct-SQL writes outside services; no hex IDs; no model calls. |
| 5 | WORKER DEBRIS | **PASS** | No TODO/FIXME/debugger/commented-out code in new sources. The dead bridge/view-model libs are billed under Defect 1, not here. |
| 6 | SCREENSHOTS | **FAIL — REWORK** | Zero work screenshots, zero Playwright runs, `work.spec.ts` missing. Fork-provenance baselines sit unused. Not desktop-only-regenerated — nothing at all, 5th consecutive cycle. Half of Defect 1. |
| 7 | SPANS (ADR 0010) | **PASS w/ deviation** | Unchanged from r39: every endpoint handler in `Effect.fn(\`Work.<name>\`)` inside the request trace; route/method/status/stellarc.org/principal.kind asserted; per-aggregate span coverage; my rename sabotage reddens (above). Deviation stands: 0 `Effect.fn` in domain work.ts — spans at HTTP boundary only; noted, not billed. T25 allowlist green. `console.*` only in `tools/` under an explicit biome allowance (b856c9b) — r39 D7 resolved. |
| 8 | GATES RE-RUN (mine) | **FAIL — NEW** | `bun install --frozen-lockfile` clean. `bun run test:unit` **35 passed**. `bun run test:integration` **99 passed (6 files, 450s)**. But `bun run typecheck` **exit 2** and `bun run lint` **exit 1** at PR head, while dev is clean (0/0 — verified by checking out origin/dev in the same worktree). Breakage is entirely this PR's new test files. See Defect 2. |

## DEFECTS

1. **[BLOCKER, carried from r39 D1] Five frozen screens still not wired to live collections; no e2e; no parity evidence — and cycle 43's response is dead code.** `work-live-bridge.ts`/`work-view-model.ts` have zero importers under `apps/stellarc-ui/src/{routes,components}`; `board.tsx:23/58`, `backlog.tsx:26`, `milestones.tsx`, `nav-boards.tsx:43/80`, task detail still read T0 react-query stubs; `apps/stellarc-ui/e2e/work.spec.ts` missing; 0 Playwright runs. **Fix:** consume the bridge from the five screens (sidebar boards, Kanban, list, backlog, ticket detail), add `work.spec.ts` with T28–T34 including per-row negative controls and 5 screens × 4 viewports parity against fork-provenance baselines on built API + isolated PG — or obtain an explicit orchestrator deferral (none exists).
2. **[BLOCKER, NEW] Both repo gates red at head: `typecheck` exit 2, `lint` exit 1 (dev: 0/0).** Root `tsconfig.json:13` excludes `apps/stellarc-ui/**`; new `tests/unit/work-view-model.test.ts:1-2` relatively imports `../types/task` + `../types/board`, dragging excluded UI files into the root program → TS2307 (`@kaneo/libs` types/board/index.ts:1, `@/types/task` :3, `@/fetchers/flag/get-task-flags` types/task/index.ts:75) plus 10× TS18048 in the test file itself (e.g. :109,:117,:126,:156). Biome: 6 errors, all in new files — work-view-model.test.ts:121 useLiteralKeys, work-import.test.ts:1 noUnusedImports, work-live-bridge.test.ts:15 noUnusedFunctionParameters, :58 noUnusedTemplateLiteral, organizeImports ×2 (work-view-model.ts:1, work-view-model.test.ts:1), formatter diffs in work-view-model.test.ts. **Fix:** make the view-model test self-contained (inline/duplicate the two type shapes instead of importing UI modules) or relocate under `apps/stellarc-ui` with its tsconfig; guard the possibly-undefined assertions (TS18048); run `bunx biome check --write` and re-run BOTH gates to 0. The implementer pushed without running typecheck/lint once.
3. **[minor] Bridge unit tests assert against a stub `LiveCollection` shape, not the real TanStack collection API** (`work-live-bridge.ts:24-31` hand-rolls the structural type). If the real collection's surface drifts, these tests stay green. **Fix:** once D1 wires real collections, add one e2e assertion that a real handle satisfies the bridge contract (T28 already mandates the live path).

**Fixed since r39 (verified, no action):** D2 alias-delete contract (40cf03b + test), D3 prod actor (2cfc06e + composition-pinned test), D4 both T37 supplementary queries (89a8a1a, work.test.ts:695), D5 T24 429 rate-limit test (1429d7a, work-http.test.ts:482 — 121st request → 429 + retryAfterSeconds), D6 self-seeding (30722fd — verified via standalone `-t T10` green), D7 biome tools allowance (b856c9b).
