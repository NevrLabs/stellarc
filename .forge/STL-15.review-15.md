REWORK

PR 53 (forge/stl-15 @ 75f3b68) vs dev merge-base 5856c24; audited in throwaway worktree /tmp/stl15-review (clean tree restored after sabotage replays). Gate check: latest implement entry = pass (2026-09-17T02:31:34Z, cycle 15, pr 53). 128 files, +9302/−1063. Massive progress over c9: spans, server-side shapes, live e2e, UI REST wiring, debris cleanup all landed — but §4's live-collection requirement is still unmet in the UI, the entire invitation lifecycle is untested, and the tablet screenshot gate flakes.

## Per-item verdicts

| # | Audit item | Verdict |
|---|---|---|
| 1 | SPEC COMPLIANCE | PARTIAL — §2 tables/events/mutations/capabilities: done. §3 endpoints: present incl. D8 schema decode, D9 405+Allow, D10 slug bounds, invitation accept (identity-http.ts:1234)/cancel (:305)/pending (:521)/details (:551). §4 shapes: nine registered server-side (identity-shapes.ts defs.set organization…principal) BUT views read React Query REST snapshots — the exact forbidden pattern (defect 1). §5 manifest: tools/import-identity.ts, identity-fixture.ts, identity-import/telemetry/grants/auth/http/shapes tests all present; `lib/auth-client.ts` + `use-auth.ts` MODIFY items untouched (pre-exist STL-14); tests/fixtures/identity-reconciliation.sql correctly NOT invented (T26–T28 blocker carried). FK org_event_counter→organization never added (defect 6). Importer source connection not read-only (defect 7). Scope creep: none found (UI fetcher/hook changes match §5 list). |
| 2 | TESTS THAT CANNOT FAIL | MIXED — replayed negative control (PASTED below) proves span tests bite. But T18/T19 invitation accept/cancel: zero tests exist (acceptInvitation/cancelInvitation absent from tests/); T13/T20/T21/T33/T35 rows have no red-capable test (defects 2, 4, 5). |
| 3 | MIGRATIONS | CLEAN — PR touches no migration file (diff vs merge-base empty for packages/db/migrations/); `git tag --contains` on 0001/0002 → no tags, unshipped; no journal reformat. |
| 4 | DOCTRINE | OK-ish — mutations take ctx.principalId (actor=principal.id); removeMember deletes human+agent grants WITH grant-deleted events (grants test); authenticateApiKey read-only ("performs NO writes" test); no hardcoded hex; no model calls; no console.* in new src (biome 0 errors). Importer actor literal "identity-importer" (import.ts:902) is a named maintenance actor, deterministic — acceptable but not a principal.id (note, not defect). Obfuscated `"stellarc.txtid".replace("txtid","txid")` at mutations.ts:31 — write the literal (defect 9). |
| 5 | WORKER DEBRIS | CLEAN — no TODO/FIXME/debugger/console in new src; .forge-notes/.forge-blocker/.forge-deps-added deleted and gitignored (c9 defect 12 remedy). .forge/* "deletions" vs dev are branch-behind-dev artifacts (dev-only commits after merge-base), not tampering. My own review probes removed; tree verified clean. |
| 6 | SCREENSHOTS | PARTIAL — 16 new baselines: members/teams/roles/developer × desktop/tablet/mobile/mobile-small; mobile projects are hasTouch+isMobile. Desktop 4/4 green ×5 runs; mobile/mobile-small green; tablet teams FLAKY: 2 of 4 runs red, 3473px diff (ratio 0.0044 > 0.001), brightness-level deltas rgb(10,10,11) vs rgb(25–32) across wide regions, no pageerror — capture nondeterminism (defect 3). members live-content assertion (getByText "Fixture Owner") is behavioral, good. frozen.spec.ts correctly drops the four promoted screens. |
| 7 | SPANS (ADR 0010) | FUNCTIONAL / LETTER DEVIATION — every mutation wrapped by tracedMutation → `Identity.<name>` spans carrying stellarc.org + txid; identity-http.ts:374/386 sets http.route + stellarc.principal.kind; auth-http.ts:55 http.route; T30 asserts route/method/status/org/principal.kind + Identity.authenticate/capabilities with traceparent TRACE_ID propagation; T31 asserts no SQL/secret attrs. NOT Effect.fn("Module.name") — manual TracerLike spans instead (defect 8). Redness proven by sabotage replay (below). No console.*, no PII/statement text in attrs (T31 asserts regexes). |
| 8 | GATES RE-RUN | GREEN — unit: 8 files 44/44, exit 0 (220.7s); integration: 8 files 88/88, exit 0 (609.5s); `bun run lint` exit 0 (985 files); `bun run typecheck` exit 0; e2e identity.spec desktop 4/4 (×5), mobile+mobile-small pass, tablet flaky per defect 3. Gates green on what exists; defects are coverage/architecture gaps. |

## Negative-control replay (item 2, pasted)

Sabotage: removed span creation in tracedMutation (mutations.ts:17 → `return body()` before startSpan):
```
RED:   vitest run --config vitest.integration.config.ts tests/integration/identity-telemetry.test.ts -t T31
       263|  const mutation = spans.find((s) => s.name === "Identity.createRole");
       264|  expect(mutation).toBeDefined();
       Tests  1 failed | 2 skipped (3)   [exit 1]
GREEN: restored → Tests  1 passed | 2 skipped (3)   [exit 0]
```
T31 span assertions are behavioral, not decorative.

## DEFECTS

1. **[MAJOR] §4 live collections unconsumed by UI.** `apps/stellarc-ui/src/lib/identity-collections.ts` is 132 lines of type definitions only — zero TanStack wiring, zero collection references; Members/Teams/Roles/Keys views read React Query REST fetchers (e.g. use-get-organization-members.ts → getOrganizationMembers → identityGet). Spec §4: "Members/Teams/Roles/Keys views must read live collections, not React Query snapshots with a decorative shape connection." Server shapes exist but have no consumer, no awaitTxId settle, no revocation-driven invalidation. Fix: wire the frozen views to the nine shapes via the TanStack adapter with org-scoped handles, or justify REST+invalidation equivalence against spec text in the PR.
2. **[MAJOR] Invitation accept/cancel untested (T18/T19).** Routes + mutations exist (identity-http.ts:1234, :305) but acceptInvitation/cancelInvitation appear in no test file. Untested: invitee/expiry/status validation, atomic consume-once under concurrency (AlreadyAccepted), managers-only capability filter. Fix: add integration tests incl. spec negative controls — remove conditional status update (T18), drop invitation capability filter (T19) — and paste RED/GREEN.
3. **[MAJOR] Tablet teams screenshot flaky (T32).** 2/4 runs red: 3473px diff (0.0044 > 0.001), brightness-level deltas across broad regions, no pageerror — animation/scrollbar/hover nondeterminism at capture on 1024×768. Fix: stabilize capture (settle invariant, mask scrollbar/hover regions or disable transitions harder) and re-capture the tablet baseline; demonstrate 5/5 consecutive green tablet runs.
4. **[MODERATE] T33 org-switch disposal untested + dead hook.** use-switch-organization.ts has zero callers; the live switcher (organization-switcher.tsx:52–73) inline-fetches /api/identity/active-org, ignores failures, invalidates no query cache, disposes nothing. Spec §4 org-switch disposal + T33 back/reload-inaccessible-org: no test anywhere. Fix: route switcher through the hook, clear org-scoped caches on success, add T33 e2e negative control (unscoped collection key reuse must go red).
5. **[MODERATE] Spec test rows with no red-capable test: T13** (dynamic-role replace-vs-union, immutable owner edit rejection), **T20** (secret returned exactly once + immediate revoke-deny), **T21** (secret leakage across snapshot/tail/old_value/REST beyond the apikey-digest test), **T35** (per-org user fanout / no account stream). Adjacent coverage exists (grants, shapes secret test, D1/D2) but the named sabotages have no test. Fix: add each row's negative control or fold into existing suites.
6. **[MINOR] FK org_event_counter.org→organization.id never added (§2).** 0001_foundation.sql unchanged; no ALTER in 0002 or importer. Spec: add after removing test-only probe data in isolated setup. Fix: add the FK in a new migration with probe-data cleanup in the disposable harness, or record the deviation with reason.
7. **[MINOR] Importer source connection not read-only (§2).** tools/import-identity.ts:47 opens a normal postgres() connection; only SELECTs run today but the read-only guarantee is unenforced. Fix: open source with `BEGIN READ ONLY` semantics (e.g. `SET default_transaction_read_only = on` on the source session) so a future write bug aborts.
8. **[MINOR] Manual spans instead of Effect.fn (ADR 0010 letter).** tracedMutation/TracerLike produce correct named spans with proven redness, but new service functions are not Effect.fn("Module.name") as required. Fix: convert identity service seams to Effect.fn, or get an ADR-level exception recorded.
9. **[MINOR] UI MODIFY stragglers + obfuscation.** lib/auth-client.ts and providers/auth-provider/hooks/use-auth.ts untouched while §5 lists them MODIFY (client still mounts magicLink/emailOTP/anonymous plugins the server doesn't); `"stellarc.txtid".replace("txtid","txid")` at mutations.ts:31 — write `"stellarc.txid"` literally.
10. **[CARRIED BLOCKER — not counted] T26–T28 reconciliation fixture still absent.** Correctly not invented; canonical #1–#3 SQL remains the orchestrator handoff blocker. Keep visible; do not mark skipped-green.

Counts: 3 MAJOR, 2 MODERATE, 4 MINOR, 1 carried blocker.

Gates re-run (mine, throwaway worktree @75f3b68): unit 44/44 exit 0; integration 88/88 exit 0; lint exit 0; typecheck exit 0; e2e desktop 4/4 ×5, mobile/mobile-small green, tablet teams flaky (defect 3).
