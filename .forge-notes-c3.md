# STL-15 c3 — implementer handoff notes

## Cycle base
Rework cycle 3. Base = c2 PR-head-equivalent `0bba857` (c2 died pre-PR; its
two green commits preserved rather than discarded). Review c1 defects
addressed this cycle: 1,2 (pre-c3, verified), 6 (D12 events), 11 (preflight),
12 (per-table events + accurate eventCount), 13 (dead code), 15 (barrel).

## TDD evidence (RED → GREEN → negative control RED → restored GREEN)
- Unit A error shape: RED `expected { _tag: 'Conflict' } to match
  { _tag: 'Unauthenticated' }` → GREEN → sabotage (conflict("Unauthenticated")
  restored) → RED ×1 → restore → GREEN. tests/unit/identity-error-shape.test.ts
- Unit B importer: 4 RED (duplicates imported silently / org-only events) →
  GREEN 4/4 → sabotage preflight-call-removed → 3 T25 RED → sabotage
  events-suppressed → D12 RED → restore → 4/4 GREEN + full unit 43/43.
  tests/unit/identity-import-preflight.test.ts
- Unit C HTTP: 6 RED (module missing) → 8/8 GREEN incl. avatar T22 →
  sabotage membership-predicate-removed → foreign-org test RED → restore →
  GREEN; avatar sabotage (shared-org check removed) run RED then restored
  GREEN. tests/integration/identity-http.test.ts
- T17 config: updated for AUTH_SECRET; asserts short-secret refusal.
  (foundation.test.ts predates this ticket; minimal necessary update.)

## Runtime proof
Boot smoke against disposable PG: /health 200, /api/identity/* 401
fail-closed, /api/auth/* reaches real Better Auth (validation 400).
lint=0 errors, typecheck=0, build=0.

## Remaining for later cycles (NOT claimed done)
- 26-contract writes: create/update role/team/invitation/apikey handlers,
  team-member add/remove, accept-invitation, PATCH member/org. Reads +
  member-delete + active-org are live.
- Effect.fn spans (T30/T31), identity shapes/collections (§4), UI wiring +
  frozen screenshots (§6), reconciliation #1-#3 fixture (still awaiting
  canonical inventory from orchestrator — .forge-question.md from c1
  unanswered).
- tests/fixtures/identity-reconciliation.sql NOT created: canonical SQL
  never supplied; spec forbids inventing numbered queries.
