# STL-16 implement c4 — questions (working continues meanwhile)

1. **UI live-wiring remainder (T28–T34).** The five frozen screens still read
   the T0 stub fixtures (`apps/stellarc-ui/e2e/fixtures.ts` routes `**/api/**`
   to static JSON; `frozen.spec.ts` marks the five STL-16 screens
   `test.fixme`). Wiring the screens to live shape collections requires the
   auth/session surface (STL-15 PR #39, currently REWORK) to resolve the org
   + bearer token for `workCollections(org, auth, baseUrl)`. Plan: land the
   backend slice + collections factories now (done), and wire the five
   screens in the next cycle once #39 merges — or do you want the wiring now
   against a test-only token path?
2. **Canonical reconciliation #4–#6 SQL** still not supplied. T35–T37 remain
   blocked test specifications per spec §7; supplementary orphan/StatusInUse
   checks (T37 supplementary, T21) are implemented and green.
