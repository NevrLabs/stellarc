# Build-speed config. mold linker + thin debuginfo.
# Deps get no debuginfo (never stepped through), workspace keeps line tables.
[target.x86_64-unknown-linux-gnu]
rustflags = ["-C", "link-arg=-fuse-ld=mold"]
