# STL-15 c4 — implementer handoff notes

Base: dev 5856c24 + merge of c3 f876539 (PR #42 branch, unmerged at dispatch).

## Fixed this cycle (all TDD w/ negative control)
- D2 capabilities.ts (authz model) — negative control: union-owner-membership
  sabotage → D2 HTTP test RED (200 instead of 403); restored GREEN.
- D8 ten-table verification — RED: destination-extra row imported silently;
  GREEN: rejects w/ "verification failed", ledger count 0 (full rollback).
- D1/D3/D4/D5/D6/D7/D9/D10/D12 per PR #48 body.
- Importer now reads typed columns (only bytea as ::text) — required for D8
  value comparison; postgres.js binds "true"/"false" text as false otherwise.

## Remaining for c5 (NOT claimed done)
- D11/T30/T31 Effect.fn spans on all new services + identity-telemetry.test.ts.
- D13 §4 sync shapes/collections (identity-shapes.ts, identity-collections.ts,
  identity-client.ts, shape allowlist).
- D14 §6 frozen UI wiring + e2e/identity.spec.ts + 4-viewport screenshots.
- D17 domain-layer ceiling-escalation test (HTTP-level D2 tests exist).
- T26–T28 reconciliation fixture: still orchestrator-blocked (c1
  .forge-question.md unanswered; canonical SQL never supplied).

## Host note
Full-suite single run impossible under co-run load (4 sibling lanes, load
8–18, initdb hook timeouts); per-file runs all green. Integration
hookTimeout raised to 120s in vitest.integration.config.ts.
