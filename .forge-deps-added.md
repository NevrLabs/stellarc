# STL-25 dependencies added under the pre-authorised rule

- `playwright@1.62.0` declared in the root `package.json`: `tools/measure-shape-concurrency.mts` imports `playwright` (chromium browser type) for the S12 browser ceiling measurement. Version pinned to the one already installed in `node_modules` as `@playwright/test@1.62.0`'s dependency (bun.lock has the matching entry; tsc could not resolve the transitive without the declaration).
