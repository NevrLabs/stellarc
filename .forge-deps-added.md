# Dependencies added (STL-15 c1, pre-authorised per brief §21)

- `bcryptjs@^3.0.2` (root package.json dependencies) — mirror source
  `apps/api/src/auth.ts` (kaneo pin 2504e645) imports bcrypt for
  emailAndPassword.password.hash/verify (fork apps/api/package.json pins
  `bcryptjs ^3.0.2`); no manifest in this repo declared it. Pinned to the
  fork's version range.
- `@better-auth/core@1.6.25` (packages/domain/package.json) — direct import of
  `createAdapterFactory` from `@better-auth/core/db/adapter`; better-auth's
  own dependency, pinned to the locked version.
