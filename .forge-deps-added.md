# STL-14 dependency boundary repairs

- `packages/contracts/src/legacy/permissions/index.ts:1,7` imports Better Auth at runtime. Declare `better-auth` in the contracts workspace, not only UI; Bun isolated workspace resolution otherwise fails Rollup.
- `packages/contracts/src/legacy/libs/hono.ts:4` imports `hono/client`. Declare `hono@4.13.7`, matching the installed UI dependency, in contracts.
- Pin `better-auth@1.6.25` in UI and contracts, matching `pnpm-lock.yaml` at the pinned fork commit `2504e64512b84b9b739d4d4fb0d4ceaefdb14783`. The previous range resolved to 1.7.3, where the mirrored `genericOAuthClient` import no longer exists. No auth behavior redesign; restore the frozen fork dependency contract.
- Regenerated `bun.lock`; built API and lifted UI successfully (2 successful tasks). Client type-only `@kaneo/api` boundary remains unresolved scope, not claimed fixed by the runtime build.

## STL-22 cycle 2 (Tauri desktop shell)

- c1 introduced `reqwest = { version = "0.12", default-features = false, features = ["blocking", "rustls-tls"] }` in `desktop/Cargo.toml` (review-1 §1: justified — implements §3 best-effort `/health` probe). Carried into c2 unchanged; no new crates in cycle 2.
- `desktop/package.json` pins `@tauri-apps/cli ^2` (lock 2.11.4) per spec §5; unchanged.
- Behaviour-affecting probe findings (recorded as tests): maestro 2.10.0 ignores `config.yaml` unless `--config` is passed (garbage-config discriminator, probes C/D/E) — pinned in `.github/workflows/desktop.yml` + T06; `option_env!(...).unwrap_or(..)` fails E0658 on stable rustc 1.98.1 (first real compile of the crate) — fixed with the const-match idiom in `desktop/src/lib.rs`.
- Toolchain-only, not repo deps: rustup stable + Maestro CLI installed locally to run/probe gates; CI pins its own (`dtolnay/rust-toolchain@stable`, get.maestro.mobile.dev).
