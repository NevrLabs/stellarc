# STL-17 c1 — dependencies added / plumbing log

- No new npm dependencies: outbox LISTEN uses the pinned `postgres` driver
  already in the lockfile (`sql.listen`); secret crypto is node:crypto
  (stdlib). Nothing pinned from the mirror's node_modules.
- packages/db/src/migrate.ts: +1 MIGRATIONS entry (0004), +grantRuntime loop
  for the nine new tables (required plumbing; see question Q3).
- STL-16/STL-19 seams are function-typed params on the domain services
  (TicketScope, BoardStatusValidator, FollowerProvider); production bindings
  land with the owning slices' integration commits.
- tests/unit/identity-contracts.test.ts D1 assertions extended: the ledger
  expectations hard-coded the migration list; 0004's entry is now expected.
  (STL-15's tests, behaviour unchanged, list contents legitimately grew.)
