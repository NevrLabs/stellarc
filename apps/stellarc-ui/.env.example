# e2e + preview parity: serve the API same-origin through the vite/preview
# proxy instead of the kaneo default http://localhost:1337 (which collides with
# a dev kaneo instance on this host and is unreachable in CI). Relative "/api"
# is the documented dev convention (see src/fetchers/get-ws-url.ts).
VITE_API_URL=/api
