export function Motion() { return <div className="space-y-3 text-sm"><p>Interaction transitions use <code>--dur-fast</code>, <code>--dur-base</code>, and <code>--dur-slow</code>; loops use shared keyframes.</p><div className="rounded border border-border p-3"><strong>Reduced motion:</strong> <code>@media (prefers-reduced-motion: reduce)</code> removes nonessential movement while preserving state changes.</div></div>; }
