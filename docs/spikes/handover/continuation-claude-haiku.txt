Now that SSO is authenticated, I'll verify the AWS credentials work, then query the Kubernetes cluster to inspect the current tempo-ingester resource requests/limits and OOM events, pull its actual memory usage patterns and peak from Prometheus/Grafana (to correlate with ingestion rate and trace volume), and cross-reference any recent OOM kills with pod restart logs—this gives me the workload profile needed to calculate proper RAM sizing with headroom for traffic spikes.
