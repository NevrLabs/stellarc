# Reverse-proxy / local HTTP-edge landscape for Stellarc

**Research date:** 2026-07-12. **Target:** an externally owned, locally supervised edge on one Ubuntu host, later multiple nodes; Stellarc creates/deletes ephemeral routes, serves artifact files, and delegates every protected request to its own authorization API.

## Comparison table

Legend: **Yes/API** = route add/remove in the running process through a documented, product-owned API (no config-file rewrite and no worker/process reload); **Yes/pull** = the running proxy watches/polls an external source, so Stellarc changes that source rather than the proxy; **Partial** = only backends/maps or a custom extension; **No/reload** = a file and graceful/hot reload is still the control mechanism. “Static” means a real filesystem/static-response handler, not “can cache an origin.” Protocol order is **WebSocket / downstream HTTP/2 / unbuffered streaming**. CVE numbers are the conservative three-year count defined below: **project-core RCE or memory-safety-class CVEs, 2023-07-12 through 2026-07-12; dependencies and appliance forks excluded**.

| Candidate | Dynamic routes at runtime | Static files | Forward-auth delegation | WS / H2 / stream | Automatic TLS / on-demand | Memory safety; 3y RCE/memory CVEs | Ubuntu process / license | Health / bus factor | Stellarc fit |
|---|---|---:|---|---|---|---|---|---|---|
| **Caddy 2** | **Yes/API**, native JSON Admin API; atomic zero-downtime updates and rollback ([docs](https://caddyserver.com/docs/api)) | **Yes**, mature `file_server` ([docs](https://caddyserver.com/docs/caddyfile/directives/file_server)) | **Yes**, `forward_auth` ([docs](https://caddyserver.com/docs/caddyfile/directives/forward_auth)) | ✓ / H2+H3 / ✓ | **Best-in-class** ACME; on-demand with authorization “ask” endpoint ([docs](https://caddyserver.com/docs/caddyfile/options#on-demand-tls)) | Go; **0** | One official binary/deb + systemd; Apache-2.0 | Very active, many contributors; commercial stewardship, but architectural leadership remains concentrated around Matt Holt | **Best fit** |
| **Envoy Proxy** | **Yes/API**, stable versioned xDS gRPC/REST; delta add/remove ([xDS](https://www.envoyproxy.io/docs/envoy/latest/api-docs/xds_protocol)) | **No** general file server (only inline/direct responses) | **Yes**, HTTP or gRPC `ext_authz` ([docs](https://www.envoyproxy.io/docs/envoy/latest/configuration/http/http_filters/ext_authz_filter)) | ✓ / ✓ / ✓ | No native ACME/on-demand; SDS accepts externally managed certs | C++; **18**, mostly medium/high UAF/OOB/crash; one potential RCE in CVE-2026-48706 | Static release binary/container; apt repo currently unmaintained ([install](https://www.envoyproxy.io/docs/envoy/latest/start/install)); Apache-2.0; xDS control-plane complexity | CNCF/LF, Google and broad vendor base, frequent multi-branch security releases; low bus factor | Excellent routing/auth; fails native-static and memory-safety preferences |
| **HAProxy** | **Partial**. Runtime API now adds/deletes dynamic backends, servers, maps, certs ([reference](https://www.haproxy.com/documentation/haproxy-runtime-api/reference/)); arbitrary frontends/rules still use Data Plane API, which **writes `haproxy.cfg` and invokes reload** ([repo example](https://github.com/haproxytech/dataplaneapi)) | No directory server; small `http-request return file` only | No first-class equivalent; Lua/SPOE/custom HTTP call | ✓ / ✓ / ✓ | Native ACME is emerging/current; not Caddy-style unrestricted on-demand | C; **1 medium** heap overflow, CVE-2025-32464 ([NVD](https://nvd.nist.gov/vuln/detail/CVE-2025-32464)) | Excellent Ubuntu packages; one proxy plus optional Go Data Plane API; GPL-2.0 | Mature core team + HAProxy Technologies; fast LTS branches, low bus factor | Strong classic proxy, but strict dynamic-route and static/auth axes fail |
| **nginx OSS** | **No** full route API. Signals perform graceful reload. NGINX Plus API changes upstream members/keyvals, not arbitrary server/location routing ([Plus API](https://nginx.org/en/docs/http/ngx_http_api_module.html)) | **Yes** | **Yes**, `auth_request` ([docs](https://nginx.org/en/docs/http/ngx_http_auth_request_module.html)) | ✓ / ✓ downstream (ordinary HTTP proxy upstream historically H1; gRPC H2) / ✓ with buffering disabled | No native ACME/on-demand; use certbot/acme companion | C; **17** memory-safety advisories (0 labelled RCE), many module/HTTP3-specific ([official list](https://nginx.org/en/security_advisories.html)) | Superb distro packages; config/reload operationally simple; BSD-2-Clause | F5 security response and large ecosystem; active releases; project governance historically concentrated | Previous judgment **verified**: static yes, strict dynamic API no |
| **OpenResty** | **Partial/DIY**: Lua/shared dictionaries can implement a route API without reload, but OpenResty does not ship a stable generic route-management API | **Yes** (nginx) | Yes via `auth_request` or Lua subrequest | ✓ / ✓ / ✓ | No native ACME; Lua modules/external tooling | C nginx + LuaJIT; **1 OpenResty-specific high OOB write** (CVE-2026-55233), plus applicable nginx-core exposure ([NVD](https://nvd.nist.gov/vuln/detail/CVE-2026-55233)) | Packages/container; substantial custom Lua ownership; BSD-2-Clause | Healthy but smaller core team; releases slower than nginx | Powerful construction kit, not an upstream-owned turnkey edge contract |
| **NGINX Unit** | **Yes/API**, entire JSON configuration and routes managed live; API is sole source of truth ([docs](https://unit.nginx.org/controlapi/)) | **Yes** | No mature generic forward-auth primitive; application/JS workaround | Limited WS proxy story / H2 / stream | TLS cert API, no native ACME/on-demand | C; **0** in class | Ubuntu packages existed; Apache-2.0 | **Archived and unmaintained since Oct 2025** ([notice](https://unit.nginx.org/)) | Once unusually close on dynamic+static, now disqualified by abandoned security ownership |
| **Apache httpd 2.4** | **No/reload**; text config and graceful restart ([config docs](https://httpd.apache.org/docs/2.4/configuring.html)) | **Yes** | Possible with modules/rewrite/FastCGI, but no clean, standard Traefik/Caddy-style HTTP forward-auth primitive | ✓ / `mod_http2` ✓ / ✓ | `mod_md` ACME, not general on-demand-per-host | C; **13** memory-safety CVEs, including one important possible RCE (CVE-2026-23918) ([official list](https://httpd.apache.org/security/vulnerabilities_24.html)) | First-class Ubuntu package; module/config complexity; Apache-2.0 | ASF team, long-lived and active; strong security process though recent C bug cluster is material | Static/mature, but dynamic hard fail and large C/module attack surface |
| **Traefik Proxy** | **Yes/pull** through supported Docker/KV/Kubernetes providers; no reload. A direct `PUT /api/providers/rest` handler also exists, but is poorly documented and should **not** be treated as a stable public CRUD contract ([dynamic methods](https://doc.traefik.io/traefik/reference/routing-configuration/dynamic-configuration-methods/), [REST source](https://github.com/traefik/traefik/blob/master/pkg/provider/rest/rest.go)) | **No** native filesystem server | **Yes**, ForwardAuth | ✓ / ✓ / ✓ | **Yes** ACME; obtains certs for discovered routers; no safe Caddy-style arbitrary-SNI on-demand mode | Go; **1 high RCE-capable** plugin archive traversal, CVE-2025-54386 ([NVD](https://nvd.nist.gov/vuln/detail/CVE-2025-54386)) | Single static binary/deb/container; simple config; MIT (Proxy remains OSS; Hub/Enterprise are commercial) | Large active Traefik Labs/community, frequent releases; low bus factor | Previous judgment **verified**: dynamic is strong, but static files are absent |
| **YARP** | **Yes/in-process**, `IProxyConfigProvider` change tokens; not a network management API and not a standalone product ([Microsoft config docs](https://learn.microsoft.com/en-us/aspnet/core/fundamentals/servers/yarp/config-files)) | **Yes**, by adding ASP.NET Core Static Files | ASP.NET auth middleware or custom HTTP authorization call; no packaged forward-auth switch | ✓ / Kestrel H2 / ✓ | ASP.NET HTTPS only; no built-in ACME/on-demand | Managed C#; **0** YARP-core | You must build, publish and supervise a custom .NET host; MIT | Microsoft/dotnet team, healthy but release cadence slower than edge proxies | Functionally composable, but makes Stellarc own an edge application—contrary to upstream-owns-response goal |
| **Pingora framework** | Whatever the application implements; **no ready route-management API** | Framework can implement it; no turnkey core server | Custom hook | Upgrade/proxy support and H2 / H2 / ✓ | No turnkey ACME/on-demand | Rust (with crypto/FFI choices); **0** class, but 3 request-smuggling/cache CVEs in 2026 | Build a Rust application; Apache-2.0 | Cloudflare-backed, active; framework API still evolving | A library, not external security ownership for Stellarc’s finished edge |
| **River (on Pingora)** | **No** API; config-file hot reload only; README says config exclusively by file and “no expectation of stability” ([README](https://github.com/memorysafety/river)) | **Yes**, basic static HTML | No | Not clearly complete / H2 / proxy streaming | ACME still roadmap | Rust; **0** | Linux binary; Apache-2.0/MIT dual intent | **Stalled at v0.5.0, last code/release 2024**; Prossimo/ISRG sponsorship did not yield 1.0 | Not production-selectable |
| **Sōzu** | **Yes/API-like IPC**, first-class protobuf command protocol over Unix socket; no reload ([README](https://github.com/sozu-proxy/sozu)) | No general file server | No first-class forward-auth | Upgrade/tunnel likely / H2 / ✓ | TLS yes; ACME story not yet Caddy-class | Rust; **0** | Signed prebuilt glibc/musl binaries; one process with master/workers; **AGPL-3.0** (LGPL command lib) | Clever Cloud-backed, very active 2026 releases; materially smaller maintainer base | Best memory-safe specialist, but misses static + auth and AGPL needs counsel |
| **rpxy** | **No/API**; watches and hot-applies a TOML file ([README](https://github.com/junkurihara/rust-rpxy)) | No | No | Not documented as a core promise / H2+H3 / ✓ | ACME TLS-ALPN automatic; no authorization-gated on-demand | Rust; **0** | Binary/container/third-party deb; MIT | Active but explicitly WIP and primarily one maintainer; high bus factor | Too narrow and strict dynamic failure |
| **lighttpd** | **No/reload**, file config | **Yes** | External auth modules exist, but not a modern generic HTTP forward-auth contract | ✓ / ✓ / ✓ | No built-in ACME | C; **0 core** (exclude vulnerable router vendor forks/CGIs incorrectly returned by keyword searches) | Excellent Ubuntu package, tiny footprint; revised BSD | Active but very small maintainer base and niche adoption | Efficient static server, wrong control model |
| **H2O** | **No/reload**, YAML config | **Yes** | mruby/custom handler; no turnkey forward-auth | Proxy upgrade support / H2+H3 / ✓ | No integrated ACME/on-demand | C; **1 high** stack-overflow-class H3/QPACK issue, CVE-2026-55213 ([NVD](https://nvd.nist.gov/vuln/detail/CVE-2026-55213)) | Source/package availability uneven; MIT | Upstream release tag is literally `tag-no-more-releases` (Jan 2026); effectively maintenance-only/ending | Disqualify on control model and lifecycle |
| **Varnish Cache** | VCL can be loaded/selected/discarded live through CLI, but that is **uploading generated config**, not route CRUD; dynamic backends need VMOD/Enterprise | No disk file origin; caches/proxies origin content | VCL/subrequest/VMOD customization, no standard primitive | Passes upgrades / H2 support / streaming-cache semantics differ | OSS does not terminate TLS; no ACME | C; **0** class in window (several desync/DoS CVEs) | Ubuntu package but normally requires TLS terminator; BSD-2-Clause | Mature commercial/community lineage, but original GitHub repo archived/moved in 2026 | Cache accelerator, not this edge |
| **sniproxy** | No; config/start/reload depending fork | **No** | **No**—cannot inspect HTTP inside passthrough TLS | TCP passthrough only; protocols opaque | No termination/ACME | C; **0** | Ubuntu package/source; BSD-2-Clause | Low cadence, small maintainer base | Categorically unsuitable: SNI/TCP router cannot serve/auth HTTP |
| **Zoraxy** | UI/internal API changes live state, but public automation API stability is weak and product is UI-first | Some web/static hosting features | Forward-auth exists | ✓ / ✓ / ✓ | ACME UI | Go; **2 RCE-capable**: command injection CVE-2024-52010 and arbitrary-write CVE-2026-33529 ([NVD 2024](https://nvd.nist.gov/vuln/detail/CVE-2024-52010), [NVD 2026](https://nvd.nist.gov/vuln/detail/CVE-2026-33529)) | Single binary/container; AGPL-3.0 | Active, but strongly single-maintainer/product-personality risk | Convenient homelab UI, weak security/API-governance fit |
| **Nginx Proxy Manager (NPM)** | Has an internal/undocumented-ish management API, but generates nginx files, runs `nginx -t`, and reloads nginx—**fails strict criterion** ([reload evidence](https://github.com/NginxProxyManager/nginx-proxy-manager/issues/2240)) | nginx can serve files only via advanced snippets/mounts; not NPM workflow | Access lists/custom snippets; no robust generic first-class ForwardAuth | nginx capabilities | **Yes** Let’s Encrypt UI | Node/Go management + C nginx; **2 high RCE** command-injection CVEs: CVE-2024-46257, CVE-2026-40519 ([NVD](https://nvd.nist.gov/vuln/detail/CVE-2024-46257), [NVD](https://nvd.nist.gov/vuln/detail/CVE-2026-40519)) | Multi-process Docker stack + DB; MIT | Popular and active, but management-layer attack surface and many open issues | Wrong abstraction and worse security surface than nginx alone |
| **Pomerium Core** | Core routes are file/env config; Zero/Enterprise provide a control plane, not a simple stable local CRUD route API | No general static server | It **is** the identity-aware proxy; policy calls its own authorize service, not arbitrary Stellarc forward-auth | ✓ / Envoy-derived H2 / ✓ | Managed cert options, but not Caddy-equivalent local on-demand | Go plus embedded Envoy C++; **0 Pomerium-core class**, but inherits applicable Envoy security exposure | One binary possible but multiple logical services/IdP setup; Apache-2.0; Enterprise commercial | Company + OSS team, active security advisories/releases | Duplicates Stellarc auth/control-plane responsibilities; still needs static server |
| **oauth2-proxy** | **No**, flags/env/file; static upstream list | **Yes**, limited static-file-server mode ([docs](https://oauth2-proxy.github.io/oauth2-proxy/)) | It is an auth sidecar (`/oauth2/auth`), not the traffic edge’s arbitrary auth caller | WebSocket supported / H2 depends deployment / streaming proxy | TLS options but no ACME/on-demand | Go; **0** RCE/memory class (several recent auth-bypass CVEs) | Official static binaries/container; MIT; usually a second process | CNCF Sandbox, active multi-maintainer | Useful only if Stellarc delegates identity to it; does not solve dynamic routes |
| **Apache Traffic Server** | Runtime records exist; route/remap changes remain file + config reload; no first-class route CRUD API | No disk-origin server (cache only) | Plugin/custom remap | ✓ / ✓ / ✓ | TLS yes, no native ACME/on-demand | C++; **0** class (multiple request-smuggling/DoS CVEs) | Ubuntu package; sophisticated multi-file config; Apache-2.0 | ASF team, active but niche/smaller than httpd | CDN cache, not ergonomic local application edge |
| **Skipper** | **Yes/pull** from Innkeeper/etcd/Kubernetes and runtime route updates ([repo](https://github.com/zalando/skipper)); built-in file source is not enough | No | OAuth/OIDC/auth filters exist, but arbitrary Stellarc HTTP forward-auth is not a simple flagship primitive | ✓ / H2 / ✓ | TLS support, no integrated ACME/on-demand | Go; **0** | Single binary, but Innkeeper/etcd adds a service; Apache-2.0 | Zalando-backed and actively released, smaller user base | Credible dynamic router; misses static/ACME and adds obscure ops surface |
| **Apache APISIX** | **Yes/API**, stable Admin API backed by etcd ([docs](https://apisix.apache.org/docs/apisix/admin-api)) | No general static file server | **Yes**, `forward-auth` plugin | ✓ / ✓ / ✓ | ACME plugin; no Caddy-style native on-demand semantics | OpenResty C/LuaJIT; **0 APISIX-core class**, plus nginx/OpenResty dependency exposure | APISIX + mandatory etcd (and OpenResty); Apache-2.0 | Apache project, broad active team/releases | Strong API gateway, but heavy for one box and fails static/memory-safety preference |
| **Kong Gateway OSS** | **Yes/API** in DB mode; Admin API CRUD. DB-less mode replaces declarative config rather than per-object CRUD | No | Custom/plugin or enterprise capabilities; not a clean OSS generic default | ✓ / ✓ / ✓ | ACME plugin | OpenResty C/LuaJIT; **0 Kong-core class**, plus nginx/OpenResty dependency exposure | Kong + Postgres/Cassandra-style state (or DB-less limitations); Apache-2.0 core, enterprise proprietary features | Large company/community, active, low bus factor | Mature but operationally excessive and no static server |

## Evaluation method and security caveats

1. **Hard filters were interpreted literally.** A graceful or “zero-downtime” reload is still a reload. Rewriting a watched YAML/TOML file is still a file rewrite. An internal UI API is not automatically a stable public automation contract.
2. **CVE window:** NVD publication dates from 2023-07-12 through 2026-07-12, cross-checked against project advisory pages. Search starting points: [NVD HAProxy](https://nvd.nist.gov/vuln/search/results?query=HAProxy&search_type=all), [Envoy](https://nvd.nist.gov/vuln/search/results?query=Envoy%20Proxy&search_type=all), [Caddy](https://nvd.nist.gov/vuln/search/results?query=Caddy%20web%20server&search_type=all), [Traefik](https://nvd.nist.gov/vuln/search/results?query=Traefik&search_type=all), [NPM](https://nvd.nist.gov/vuln/search/results?query=Nginx%20Proxy%20Manager&search_type=all), and the [official nginx](https://nginx.org/en/security_advisories.html) and [Apache httpd](https://httpd.apache.org/security/vulnerabilities_24.html) lists.
3. **Count inclusion:** command injection/arbitrary write with demonstrated or direct RCE potential; buffer overflow/underflow/overread, UAF, double-free, OOB read/write, and stack-overflow-class defects. Pure DoS/resource exhaustion, request smuggling, auth bypass, XSS, SSRF, and dependency-only CVEs were not counted. Thus **0 does not mean secure**. For example Pingora has three 2026 request-smuggling/cache CVEs, oauth2-proxy has recent auth bypasses, and Caddy has CVE-2026-27589 involving its local Admin API, but none belong in this deliberately narrow count.
4. **Exposure is configuration-dependent.** Several nginx defects require HTTP/3 or optional modules; many Envoy defects require a specific filter; several Apache defects require optional proxy modules or a malicious upstream. Counts measure security-response burden and unsafe-code surface, not expected exploit frequency in Stellarc’s exact config.
5. **Dependencies matter operationally even when excluded from counts.** Go/Rust projects can still inherit crypto/parser CVEs; Pomerium embeds Envoy; OpenResty/APISIX/Kong inherit nginx/LuaJIT risk. Pin and scan the full shipped artifact/SBOM.
6. **Response-time evidence is uneven.** Apache’s page publishes report/fix/release dates (examples in 2026 range from roughly one day to three months). Envoy typically fixes several maintained branches in coordinated releases and has a Linux Foundation security team/Google patch-reward process. nginx routes reports through F5 SIRT. For smaller Rust/Go projects, “0 CVEs” often means a shorter exposure history and smaller researcher audience, not proven faster response.

### Security-response owner and observed posture

There is no comparable public mean-time-to-remediate dataset and most projects promise no SLA. “Fast” below means fixes normally ship in a coordinated advisory/current release or maintained branches; it is not a contractual response time.

| Candidate(s) | Who owns response | Observed posture / timing evidence |
|---|---|---|
| Caddy | Caddy project maintainers; Go team for runtime/stdlib | Private reporting and active releases; coordinated fixes, no public SLA. Small core leadership relative to Envoy/nginx. |
| Envoy | Envoy OSS Security Team under Linux Foundation/CNCF ecosystem; Google participates in patch rewards | **Strong/fast:** coordinated advisories and patched releases across several supported minor branches, often on disclosure day. |
| HAProxy | HAProxy core maintainers and HAProxy Technologies; distro security teams for packages | **Strong:** active LTS maintenance and rapid upstream fixes; no public SLA. CVE-2025-32464 was fixed in current branches around coordinated disclosure. |
| nginx / NGINX Plus | F5 SIRT and nginx maintainers | **Strong/centralized:** official signed patches/advisories and fixed releases are normally coordinated. The 2026 cluster demonstrates both active auditing and a high C-code patch burden. |
| OpenResty | OpenResty maintainers; nginx/F5 for inherited core | Active, but a smaller team and slower release train than nginx; no response SLA. Consumers may wait for OpenResty to absorb nginx fixes. |
| NGINX Unit | **Nobody actively maintaining upstream** after archival | Unacceptable: no future feature or security-fix commitment. |
| Apache httpd / Traffic Server / APISIX | Each Apache PMC plus Apache Security Team | Formal private disclosure process and published advisories. httpd’s official table shows 2026 report-to-release intervals from days to about three months; mature but not uniformly quick. |
| Traefik | Traefik Labs and community security maintainers | **Strong/fast:** GitHub advisories generally accompany patched active-branch releases; no SLA. High 2026 advisory volume requires prompt upgrades. |
| YARP | Microsoft .NET/YARP team and MSRC/.NET servicing | Strong institutional owner and supported .NET servicing, but Stellarc would own vulnerabilities in its custom host/middleware. |
| Pingora | Cloudflare Pingora maintainers | Strong corporate expertise and active releases, but the OSS framework has a short history and no finished-application security owner. |
| River | Prossimo/ISRG project repository in principle | Practically dormant since 2024; no credible current response cadence. |
| Sōzu | Clever Cloud and Sōzu maintainers | Active signed releases and current development; smaller security audience/team, no SLA. |
| rpxy | Primarily its individual code owner | README explicitly says best-effort, personal-interest maintenance; private reporting exists but bus factor and response guarantee are weak. |
| lighttpd | lighttpd core maintainers and distro maintainers | Active and lean, but a very small upstream team; no formal response SLA. Ubuntu may backport separately. |
| H2O | Original H2O maintainers | “No more releases” makes future response uncertain despite source activity; do not depend on it. |
| Varnish | Varnish Cache maintainers/Varnish Software and distro teams | Historically formal advisories and coordinated OSS/Enterprise releases; repository transition/archive adds current governance uncertainty. |
| sniproxy | Small community/individual maintainers | Low cadence and no visible security-response organization or SLA. |
| Zoraxy | Predominantly the project’s individual maintainer | Fixes/releases are active, but two RCE-capable management-plane CVEs and concentrated ownership are adverse evidence. |
| Nginx Proxy Manager | Community project maintainers; nginx/F5 only owns the underlying nginx layer | No SLA; management-plane RCEs are NPM’s responsibility, not F5’s. Added surface materially weakens the ownership story. |
| Pomerium | Pomerium Inc./OSS maintainers; Envoy team for embedded Envoy | Active security mailbox/advisories and releases; response is split across Pomerium and embedded Envoy. |
| oauth2-proxy | CNCF Sandbox maintainers | Active coordinated GitHub advisories and releases; recent auth-bypass volume means fast upgrades matter even though narrow RCE/memory count is zero. |
| Skipper | Zalando and Skipper maintainers | Active releases and corporate production use; smaller external security audience, no SLA. |
| Kong | Kong Inc./OSS maintainers; OpenResty/nginx owners for dependencies | Strong commercial owner and active releases, but response responsibility is layered and enterprise/OSS fixes must be checked separately. |

## Per-candidate notes

### 1. Caddy — the only mature candidate that natively closes both discriminating gaps

Caddy’s Admin API is not a wrapper around config-file generation. Its native JSON is the runtime object model; `POST`/`PUT`/`PATCH`/`DELETE` mutate subtrees, individual requests are ACID, updates are provisioned before commit, failure rolls back, and traffic continues. Stellarc should assign stable `@id` values to each session route and make one atomic subtree update per session. Caddy persists the latest successful configuration automatically; `--resume` restores it after restart.

It also has the exact adjacent primitives: `file_server`, `forward_auth`, WebSocket-aware reverse proxying, streaming/full duplex, and automatic HTTPS. Its on-demand TLS `ask` endpoint is particularly aligned: Stellarc can authorize certificate issuance for a hostname before Caddy asks an ACME CA, preventing an attacker from consuming rate limits for arbitrary SNI.

Operationally, bind the Admin API to a Unix socket or loopback, do not expose it through public routes, and make Stellarc the only writer. The API documentation labels the multi-request concurrency approach experimental, so perform one atomic route-array/subtree replacement or serialize writes; do not implement read-modify-write from multiple controllers.

**Security ownership:** Caddy maintainers own the server and release binary; Go owns the runtime/stdlib. The reviewed period had zero project-core RCE/memory-corruption-class CVEs, though CVE-2026-27589 is a reminder that the local Admin API is privileged and browser-origin hardening matters. Go removes classic UAF/buffer-overflow risk from ordinary Caddy code, but plugins and unsafe/crypto dependencies still require scanning.

### 2. Envoy — strongest control-plane protocol, weakest match on the two operator discriminators

xDS is the most mature multi-node dynamic configuration protocol in this set. It provides ACK/NACK, versioned resources, state-of-the-world and delta transports, removal semantics, warming, and a natural path from one Envoy to a fleet. `ext_authz` is richer than most forward-auth implementations and supports HTTP and gRPC, per-route settings, metadata, request-body limits, fail-open/closed choices, and header propagation.

The cost is substantial. Stellarc must implement or embed an xDS management server and reason about LDS/RDS/CDS/EDS consistency. Envoy does not serve a filesystem tree, so artifacts need a second server/process (or Stellarc itself), violating the stated single external-edge preference. Envoy does no ACME; SDS is certificate delivery, not issuance. Packaging is also less polished on bare Ubuntu now that the official apt repository is unmaintained, though static binaries and containers are available.

**Security ownership:** response ownership is excellent—CNCF/Linux Foundation plus major vendors, coordinated supported-branch releases, clear advisories—but the proxy is a huge C++ codebase. The narrow review found 18 memory-safety-class issues, usually configuration-specific and crash-oriented, with CVE-2026-48706 explicitly noting heap-write overflow and potential RCE. If memory safety is heavily weighted, “a strong team patches C++ quickly” is still worse prevention than a mature Go/Rust edge.

### 3. HAProxy — runtime API progress does not fully overturn the earlier finding

HAProxy’s current Runtime API is much more capable than old comparisons imply: dynamic backends and servers can be added/deleted, ACL/map entries can change transactionally, and certificates can be updated in memory. A design that predeclares a generic frontend and routes `Host` through a runtime map to predeclared/dynamic backends may satisfy Stellarc without a reload. That is a valid constrained architecture.

It is not equivalent to arbitrary route CRUD. Structural rule/listener changes still go through the separate Data Plane API, whose own invocation example requires write access to `haproxy.cfg` and a reload command. Static artifact service and generic forward-auth are also not first-class. Lua/SPOE can fill both gaps, but then Stellarc owns more proxy extension code and its security behavior.

HAProxy’s C security record in this narrow period is notably better than nginx/Apache/Envoy: one medium heap overflow in an uncommon `regsub` configuration. HAProxy Technologies and the community maintain LTS lines actively. It remains an excellent conventional load balancer, just not the cleanest fit for this control-plane contract.

### 4. nginx family

* **nginx OSS:** The earlier decision is correct. It is an excellent static server and `auth_request` exactly models forward-auth. It still has no OSS route CRUD API. A graceful reload is reliable and normally zero-downtime, but sessions several times per hour would make config generation/reload part of Stellarc’s correctness boundary. NGINX Plus narrows the gap for upstream membership and key-value routing, not arbitrary location/server structure.
* **OpenResty:** One can build a genuinely dynamic router in Lua backed by shared dictionaries and expose an Stellarc-facing endpoint. That is architecture work, not a stable upstream contract. It undermines the stated reason to externalize the proxy: Stellarc becomes owner of route-table concurrency, persistence, validation and part of request parsing/auth logic.
* **NGINX Unit:** Its live Control API plus static service made it a serious conceptual fit, and it refutes any blanket claim that “all nginx-family products require reload.” However, Unit itself says it is archived and unmaintained as of October 2025. That directly violates the requirement that an external upstream own future CVE response.
* **Nginx Proxy Manager:** management UI/API does not change nginx’s underlying control plane. It generates files and reloads nginx, while adding Node, a database, privileged certificate automation, and two recent RCE-class command-injection issues. It is a homelab convenience layer, not an architecture simplifier here.

### 5. Traefik — the earlier “dynamic yes, static no” judgment stands

Traefik’s supported provider model satisfies no-reload dynamic desired state when Stellarc writes a supported KV store (or manages Docker/Kubernetes metadata). Traefik also still contains a direct REST provider that accepts `PUT /api/providers/{provider}`, but current documentation barely exposes it; a 2026 advisory shows why its `insecure=false` boundary matters. Treat that write endpoint as an unstable/internal integration unless Traefik Labs explicitly commits to it. For the ADR, the honest trade is therefore either an extra KV process or dependence on an under-documented handler—not the clean, stable Caddy Admin API contract.

The blocker is static service. Traefik intentionally routes to services; it does not map a URL path to local disk. A tiny static sidecar would fix this operationally, but then the deliverable is two supervised processes. ForwardAuth and ACME are excellent. Traefik is Go and has broad commercial/community ownership, but recent advisories—especially route/auth bypasses outside the narrow CVE count—mean the ForwardAuth integration must be tested adversarially across path normalization and forwarded-header behavior.

### 6. Microsoft YARP

YARP can satisfy every functional item if Stellarc authors a small ASP.NET Core host: an in-memory `IProxyConfigProvider`, Static Files middleware, a custom authorization middleware that calls Stellarc, Kestrel protocols, and an ACME library or external issuer. That host can expose exactly the desired route API.

But this changes the buy/build boundary. Microsoft owns YARP and .NET vulnerabilities; the Stellarc team owns composition order, auth fail-open behavior, the route API, persistence, certificate integration, packaging, and edge-level tests. It is less independent than using Caddy and adds a .NET runtime to a Rust product. It is a good choice only if deep custom behavior is strategically valuable.

### 7. Rust entrants: Pingora, River, Sōzu and rpxy

* **Pingora:** Cloudflare operates it at extraordinary scale, and Rust is attractive, but open-source Pingora is a framework. Selecting it means building the edge—the opposite of externalizing security ownership. Framework CVE response does not cover Stellarc’s routing/auth/static application logic.
* **River:** It was explicitly intended to turn Pingora into a memory-safe general reverse proxy and reached hot-reload plus basic static serving. The published roadmap still places ACME and a stable external dynamic API in the future; the README says v0.5.0 has no stability expectation and file-only configuration. No project activity since 2024 makes it non-viable now.
* **Sōzu:** This is the most credible ready Rust proxy here. Its Unix command socket and protobuf command library are a genuine first-class, no-reload runtime control interface, and Clever Cloud is shipping signed binaries actively. However it does not natively provide artifact file serving or generic forward-auth, and its AGPL-3.0 license needs review if Stellarc productizes or modifies it. The command library’s LGPL helps an external controller, but functionality remains missing.
* **rpxy:** Strong modern protocol/ACME feature set in a small Rust binary, but route changes are a watched TOML rewrite, forward-auth/static are absent, and the maintainer explicitly describes it as WIP/best-effort and personally driven.

### 8. Legacy/specialized servers

* **lighttpd:** superb small static server, but file/reload configuration and no modern control API.
* **H2O:** technically advanced H2/H3/static/proxy server, but no runtime route API and upstream has announced no more releases.
* **Varnish:** VCL can be loaded without restarting the daemon, but that is generated configuration. It is primarily an HTTP cache and needs a TLS terminator in OSS; it neither serves Stellarc artifact directories nor owns ACME.
* **Apache Traffic Server:** similarly optimized for proxy/cache/CDN workloads; remap configuration reload and plugin complexity make it a poor local app router.
* **sniproxy:** TLS passthrough cannot call HTTP auth or serve HTTP files; eliminate before scoring.

### 9. Identity-aware and management layers

* **Pomerium:** a high-quality identity-aware proxy, but Stellarc explicitly wants its own API consulted per request. Pomerium would introduce an IdP/session/policy/databroker control plane and embedded Envoy while still lacking artifact files and simple local route CRUD in Core. It solves a different problem.
* **oauth2-proxy:** useful as the endpoint that nginx/Traefik/Caddy consults, not as this edge. It has no dynamic routing API or ACME. Its limited static mode does not rescue the architecture.
* **Zoraxy:** unusually broad all-in-one homelab features, but weak public API guarantees, concentrated maintenance, AGPL, and two RCE-capable management features in the window are poor matches for a security-sensitive control plane.

### 10. API gateways and dynamic routers

* **Skipper:** technically credible; route sources update at runtime and Zalando uses it. Innkeeper/etcd is another component, static/ACME are absent, and the ecosystem is much smaller than Traefik/Envoy/Caddy.
* **Apache APISIX:** the Admin API is genuinely stable route CRUD and changes are real-time. The forward-auth and ACME plugins are relevant. Mandatory etcd plus OpenResty makes it multiple processes and exposes the C nginx/LuaJIT dependency surface; no static file origin.
* **Kong:** similarly mature Admin API in database mode, but adds a database/control plane and still lacks native static service. Its feature/licensing boundary between OSS and Enterprise must be checked plugin-by-plugin; there has not been a HashiCorp-style relicensing of the Apache-2.0 OSS core in this review.

## Adversarial top three

### 1. Caddy — recommend for the ADR and prototype

**Why it wins:** It is the only mature, actively maintained external process in the survey that simultaneously offers a first-class live route API, native artifact files, native per-request forward-auth, excellent WebSocket/H2/H3/streaming, and automatic/on-demand TLS. Go materially reduces memory-corruption risk, packaging is one binary, and multi-node evolution can use one Caddy per node with Stellarc broadcasting the same atomic desired state; distributed certificate storage is available through storage modules.

**Strongest argument against it:** The Admin API’s object model becomes a vendor-specific control-plane contract, and leadership/bus factor is less diversified than Envoy/nginx. Multi-node config distribution is **not** itself a consensus/control-plane product: Stellarc must own fan-out, idempotency, convergence, rollback visibility, and certificate-storage design. Also, safe concurrent multi-request config edits remain an area where the docs explicitly warn about collisions; use serialized/atomic subtree updates.

**Required proof of concept:** 1,000 create/delete cycles while WebSockets and streaming responses remain open; crash/restart with `--resume`; forward-auth deny/allow/header propagation and fail-closed timeout tests; path traversal/symlink tests for artifact roots; on-demand-TLS `ask` authorization and rate-limit tests; two-node convergence with one node unavailable.

### 2. Envoy — choose only if multi-node control-plane rigor outranks static/memory safety

**Why it is second:** xDS is by far the strongest long-term fleet protocol, and `ext_authz` is the richest auth-delegation contract. If Stellarc expects a serious multi-node data plane, delta xDS, ACK/NACK, versioning and resource warming reduce future reinvention.

**Strongest argument against it:** It directly fails the two previously identified discriminators: no filesystem server and no ACME. It therefore requires at least an artifact sidecar and certificate controller. More importantly for this ADR, it is a large memory-unsafe C++ edge with 18 reviewed memory-safety-class CVEs in three years, including one potential RCE. “Upstream responds quickly” is good incident response, not memory-safety prevention. xDS also creates substantially more Rust control-plane code than Caddy’s JSON API.

**Decision condition:** Select Envoy only if the team explicitly accepts (a) a second artifact server, (b) external certificate management, (c) the C++ security burden, and (d) implementing/testing a real xDS server now for fleet-scale benefits later.

### 3. Traefik — operationally simple alternative if one tiny artifact sidecar is acceptable

**Why it is third:** Its provider model applies dynamic desired state without reloads or config-file rewrites; Stellarc can write a supported KV store, while the under-documented direct REST provider is available only if explicitly accepted as a compatibility risk. ForwardAuth, WebSockets/streaming, automatic ACME and a single Go data-plane binary are mature. It is much simpler than Envoy/APISIX/Kong and has broad upstream ownership.

**Strongest argument against it:** The earlier static-file objection is fully valid. Adding a file server is architecturally small but destroys the “one supervised edge process” property and creates route/auth boundary questions (does auth happen before the sidecar, can files be reached directly, who sets cache/range headers?). Traefik also has a recurring class of path-normalization/forwarded-header/auth-bypass advisories; because Stellarc depends on ForwardAuth as a security boundary, this history deserves more weight than its narrow memory/RCE count of one.

**Decision condition:** Pick Traefik over Caddy only if provider-driven desired state is preferred to direct Admin API mutation and the team is comfortable supervising a loopback-only static server.

## Decision

**Use Caddy 2 as the default ADR choice.** The prior discriminating-axis findings are accurate: **Traefik is dynamically configurable but is not a static file server; nginx is a static file server with excellent `auth_request` but nginx OSS has no first-class arbitrary route CRUD API.** HAProxy’s newer Runtime API narrows—but does not erase—the dynamic gap. NGINX Unit once combined dynamic API and static service, but its 2025 archival makes it unacceptable under the explicit external-CVE-owner requirement.

Keep the Stellarc-to-edge interface vendor-neutral internally: define a Rust `EdgeDriver` around desired `Route { id, host/path, upstream, auth_policy, artifact_root, tls_policy }` state, implement Caddy first, and preserve the option for a future xDS driver. Do not expose Caddy’s JSON shape throughout the domain model.
