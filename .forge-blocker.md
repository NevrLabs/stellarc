# STL-18 implement — SPEC GAP BLOCKER (cycle 1)

Branch `forge/stl-18-c1`, base `dev` @ `5779c556`.

Stopped rather than invent. The spec (`.forge/STL-18.spec.md`) is materially stale and presupposes sibling foundations that are NOT merged on `dev`. Build-order §8 step 1 itself lists four unresolved external prerequisites ("Rebind to merged T0–T3 contracts, obtain canonical #10, pin provider/API schemas, resolve token encryption"). Every deliverable file in §5 depends on at least one gap below, so no unit of work can reach assertion-RED→GREEN with a negative control without choosing behaviour that belongs to an unmerged ticket or fabricating data the spec forbids.

## Gap 1 — `integration.board_id` FK target `board` does not exist (STL-16 unmerged)

- Spec §2: `integration` = `board_id:t FK`, unique `(board_id,type)`; §3 `PUT /api/identity/orgs/:org/integrations` takes `{boardId,…}`.
- `packages/db/src/migrate.ts:9-12` — `MIGRATIONS` lists ONLY `0001_foundation`, `0002_identity`.
- `packages/db/migrations/0002_identity.sql` — defines `organization`, `user`, `principal`, `identity_grant`, `identity_import`, … NO `board`.
- `.forge/STL-16.json` — latest stage `spec` pass; implement FAILED 2026-09-14T06:12 ("Failed to create agent"). No `board` table on `dev`.

Cannot create `0005_repository.sql` (the six-table migration, spec §5) without inventing a `board` table that is STL-16's scope. T05 depends on this FK being real.

## Gap 2 — authz / capability baseline (STL-15) unmerged; "authorized" semantics undefined

- Spec §3: "authorized repository discovery", "Installation/grant management requires the appropriate identity capability", "repository reads require resource access", "Foreign-org identifiers deliberately return 404".
- `packages/domain/src/authz.ts:15-18` — `AuthzLive.authorize` returns `forbidden`/`unauthenticated` unconditionally (stub).
- `apps/stellarc-api/src/http.ts:161-163` — "real identity arrives with STL-15 … production parses no tokens (§3)."
- `.forge/STL-15.json` — latest stage `spec` pass; implement dispatched 2026-09-14T06:10 (PR #39) but NOT merged.

STL-15 is declared OUT of scope (§1) yet owns the capability + per-resource access semantics every repository endpoint/sync shape needs. The current `Authz.authorize(org, headers)` cannot express per-repo resource access or per-capability checks, so T02/T05/T06/T13 cannot be implemented against a real predicate.

## Gap 3 — canonical reconciliation query #10 not supplied

- Spec §7 T10 + §5 `tests/fixtures/repository-reconciliation.sql` — "canonical reconciliation query #10 (obtain exact inventory; never invent it)".
- Spec §7: T10 "blocked until canonical SQL is supplied, never substituted with a fabricated query."

No such SQL exists in the checkout. Cannot fabricate it.

## Gap 4 — token / config encryption mechanism unresolved

- Spec §2: "encrypted-at-rest/token redaction for grants and config"; §8 step 1 "resolve token encryption".
- No key-management or encryption primitive exists in the foundation; `github_user_grant.access_token`/`refresh_token` and `integration.config` are plain-text candidates.

T06 needs a real encryption key/scheme and a CI-safe way to assert "raw secret leaks" negatively.

## Gap 5 — spec premise "merged T0/T1/T2/T3 contracts" is false

- Spec §1/§5: "use the merged T0/T1/T2/T3 Effect, authz, event, and shape contracts"; §5 names `packages/db/migrations/0005_repository.sql` (implying 0003/0004 exist).
- Reality on `dev` (`5779c556`): only T0 (STL-14, #28) and identity data contracts (STL-31, #37) are merged. Migrations 0003/0004 do NOT exist; `board` (STL-16) and the authz runtime (STL-15) are absent.

## What IS available (for the amended spec)

- T0 (STL-14): Effect `HttpApi`, event log (`event`/`org_event_counter`), embedded `ShapeEngine` — hard-wired to `sync_probe` only (`packages/sync/src/index.ts:176`); upcasters support only `foundation:probe-*` (`packages/sync/src/upcasters.ts:36-41`).
- STL-31 identity data contracts: `organization` (incl. `repos_enabled`, `default_resource_privilege`), `user`, `account`, `principal`, `identity_grant`, `identity_import`, `organization_member`, `apikey`, `session`.

## Requested amendments

1. Repoint/defer the `board` FK target for `integration` (or defer `integration` + its endpoints/events/tests until STL-16 lands).
2. Specify the authz contract STL-18 must consume (per-org capability + per-repo resource access); state whether STL-18 may read `identity_grant`/`organization` directly or must await STL-15's runtime.
3. Supply canonical reconciliation query #10 (or the pinned Kaneo inventory to derive it).
4. Specify token/config encryption scheme + key source, and how tests assert the "raw secret leaks" negative without a CI key.
5. Confirm migration numbering/sequence given 0003/0004 are absent.
