# Seed templates (ADR 0005)

- `agent/coding-agent` — general coding agent per supported harness
- `project/starter` — context skeleton + coding agent + review workflow
- `workflow/review-gate` — session.completed → review → notify
- `workflow/heartbeat` — cron re-prompt into an existing session
